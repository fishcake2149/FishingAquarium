(() => {
'use strict';

const TILE=32;
const CONFIG=Object.freeze({
  baseH:720,minAspect:1.25,maxAspect:2.45,fixedStep:1/60,maxFrameDelta:.25,
  playerSpeed:170,interactDistance:96,castMinDistance:58,castMaxDistance:430,
  biteMin:1.4,biteMax:3.7,gameMinutesPerRealSecond:1,
  saveKey:'fishingAquarium_v31',legacyV3Key:'fishingAquarium_v3',saveVersion:31,
  villageCols:112,villageRows:72,aquariumCols:64,aquariumRows:34,shopCols:34,shopRows:24
});
const BAR_ACCEL=Object.freeze({releaseStartSpeed:.01785*60,pressStartSpeed:.01785*60,maxSpeed:.0396*60,pressTurnAccel:.00135*60*60,releaseTurnAccel:.00135*60*60,pressHoldAccel:.00026*60*60,releaseHoldAccel:.00026*60*60});
const WEATHER=Object.freeze({sunny:{id:'sunny',name:'맑음',icon:'☀',weight:50},cloudy:{id:'cloudy',name:'흐림',icon:'☁',weight:30},rain:{id:'rain',name:'봄비',icon:'☂',weight:20}});
const DEPTH=Object.freeze({near:{id:'near',name:'근거리',label:'얕은 물',color:'#8bd3c5'},mid:{id:'mid',name:'중거리',label:'중간 수심',color:'#58a1b4'},far:{id:'far',name:'원거리',label:'깊은 물',color:'#326784'}});
const SPECIES=Object.freeze({
  minnow:{id:'minnow',name:'피라미',rarity:'일반',difficulty:1,price:10,weight:24,depths:['near','mid','far'],weather:['sunny','cloudy','rain'],time:'all',size:[6,15],pattern:'minnow',icon:'🐟'},
  loach:{id:'loach',name:'미꾸라지',rarity:'일반',difficulty:1,price:12,weight:20,depths:['near'],weather:['sunny','cloudy','rain'],time:'all',size:[8,22],pattern:'loach',icon:'〰'},
  crucian:{id:'crucian',name:'붕어',rarity:'일반',difficulty:1,price:15,weight:22,depths:['near','mid','far'],weather:['sunny','cloudy','rain'],time:'all',size:[10,36],pattern:'crucian',icon:'🐟'},
  snail:{id:'snail',name:'우렁이',rarity:'일반',difficulty:1,price:18,weight:15,depths:['near'],weather:['rain'],time:'all',size:[2,6],pattern:'snail',icon:'🐌'},
  waterBug:{id:'waterBug',name:'물장군',rarity:'일반',difficulty:2,price:22,weight:12,depths:['near','mid'],weather:['sunny'],time:'all',size:[4,8],pattern:'waterBug',icon:'◆'},
  frog:{id:'frog',name:'개구리',rarity:'일반',difficulty:2,price:24,weight:10,depths:['near','mid','far'],weather:['rain'],time:'all',size:[4,10],pattern:'frog',icon:'🐸'},
  carp:{id:'carp',name:'잉어',rarity:'일반',difficulty:2,price:25,weight:14,depths:['mid','far'],weather:['sunny','cloudy','rain'],time:'all',size:[18,58],pattern:'carp',icon:'🐟'},
  shrimp:{id:'shrimp',name:'민물새우',rarity:'고급',difficulty:2,price:28,weight:9,depths:['near','mid'],weather:['sunny','cloudy','rain'],time:'all',size:[2,8],pattern:'shrimp',icon:'🦐'},
  divingBeetle:{id:'divingBeetle',name:'물방개',rarity:'고급',difficulty:2,price:32,weight:8,depths:['near','mid'],weather:['sunny','cloudy','rain'],time:'night',size:[2,5],pattern:'beetle',icon:'⬟'},
  goldfish:{id:'goldfish',name:'금붕어',rarity:'고급',difficulty:2,price:40,weight:4,depths:['mid','far'],weather:['sunny','cloudy','rain'],time:'all',size:[7,24],pattern:'goldfish',icon:'🐠'},
  salamander:{id:'salamander',name:'도롱뇽',rarity:'고급',difficulty:3,price:36,weight:5,depths:['near','mid'],weather:['rain'],time:'all',size:[5,18],pattern:'salamander',icon:'≋'},
  softshell:{id:'softshell',name:'자라',rarity:'고급',difficulty:3,price:38,weight:6,depths:['mid'],weather:['sunny','cloudy','rain'],time:'all',size:[12,42],pattern:'softshell',icon:'🐢'},
  snakehead:{id:'snakehead',name:'가물치',rarity:'고급',difficulty:3,price:40,weight:7,depths:['far'],weather:['sunny','cloudy'],time:'all',size:[25,72],pattern:'snakehead',icon:'🐟'},
  bullhead:{id:'bullhead',name:'동자개',rarity:'고급',difficulty:3,price:45,weight:5,depths:['mid','far'],weather:['sunny','cloudy','rain'],time:'night',size:[12,38],pattern:'bullhead',icon:'🐟'}
});
const ITEMS=Object.freeze({wideBait:{id:'wideBait',name:'와이드 미끼',type:'bait',price:3,sell:1,stack:99,icon:'◯',description:'Catch Bar 크기 +20%'},curiosityBait:{id:'curiosityBait',name:'호기심 미끼',type:'bait',price:3,sell:1,stack:99,icon:'✦',description:'고급 생물 출현 가중치 증가'},iceBomb:{id:'iceBomb',name:'얼음 폭탄',type:'active',price:80,sell:40,stack:20,icon:'❄',description:'낚시 중 약 3초 동안 생물 이동 속도 감소'}});
const RODS=Object.freeze({basic:{id:'basic',name:'기본 낚싯대',barMult:1,buy:0,sell:40,description:'튼튼한 기본 낚싯대'},wide:{id:'wide',name:'와이드 낚싯대',barMult:1.5,buy:250,sell:150,description:'Catch Bar 기본 크기 +50%'}});
const BOBBERS=Object.freeze({basic:{id:'basic',name:'기본 찌',biteMult:1,controlMult:1,description:'표준형 찌'}});
const BUILD_DEFS=Object.freeze({smallTank:{id:'smallTank',name:'소형 수조',icon:'▣',tw:2,th:1,rotatable:true,solid:true,capacity:4},rug:{id:'rug',name:'봄빛 러그',icon:'▦',tw:3,th:2,rotatable:true,solid:false,capacity:0},plant:{id:'plant',name:'화분',icon:'♣',tw:1,th:1,rotatable:false,solid:true,capacity:0}});

const shell=document.getElementById('gameShell');
const canvas=document.getElementById('worldCanvas');
const ctx=canvas.getContext('2d');
const fishingCanvas=document.getElementById('fishingCanvas');
const fctx=fishingCanvas.getContext('2d');
ctx.imageSmoothingEnabled=false;fctx.imageSmoothingEnabled=false;
const dom=Object.freeze({dateText:document.getElementById('dateText'),timeText:document.getElementById('timeText'),weatherText:document.getElementById('weatherText'),questTitle:document.getElementById('questTitle'),questText:document.getElementById('questText'),locationText:document.getElementById('locationText'),moneyText:document.getElementById('moneyText'),bagUsage:document.getElementById('bagUsage'),bagGrid:document.getElementById('bagGrid'),interactionHint:document.getElementById('interactionHint'),castGauge:document.getElementById('castGauge'),castFill:document.getElementById('castFill'),castMarker:document.getElementById('castMarker'),castDepthHint:document.getElementById('castDepthHint'),toast:document.getElementById('toast'),resetButton:document.getElementById('resetButton'),buildButton:document.getElementById('buildButton'),buildBar:document.getElementById('buildBar'),buildName:document.getElementById('buildName'),buildStatus:document.getElementById('buildStatus'),rotateBuild:document.getElementById('rotateBuild'),cancelBuild:document.getElementById('cancelBuild'),modalLayer:document.getElementById('modalLayer'),modalBackdrop:document.getElementById('modalBackdrop'),modalClose:document.getElementById('modalClose'),modalContent:document.getElementById('modalContent'),fishingOverlay:document.getElementById('fishingOverlay'),fishingSpecies:document.getElementById('fishingSpecies'),fishingDifficulty:document.getElementById('fishingDifficulty'),catchFill:document.getElementById('catchFill'),iceBombButton:document.getElementById('iceBombButton'),iceBombCount:document.getElementById('iceBombCount'),sceneFade:document.getElementById('sceneFade')});
const VIEW={w:1280,h:720},input={keys:new Set(),pointerDown:false,sx:0,sy:0,wx:0,wy:0},camera={x:0,y:0};
let ui={modal:null,toast:0,lastHud:-1,shopTab:'buy'},cast={mode:'idle',value:0,dir:1,landing:null,depth:null,biteTimer:0,pulse:0},fishing=null,buildState={active:false,type:null,rot:0,tx:0,ty:0,valid:false},saveClock=0,visitorClock=3,uidCounter=1;

const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const lerp=(a,b,t)=>a+(b-a)*t;
const dist=(ax,ay,bx,by)=>Math.hypot(ax-bx,ay-by);
const rect=(x,y,w,h)=>({x,y,w,h});
const rectHit=(a,b)=>!(a.x+a.w<=b.x||a.x>=b.x+b.w||a.y+a.h<=b.y||a.y>=b.y+b.h);
const rectContains=(r,x,y,p=0)=>x>=r.x-p&&x<=r.x+r.w+p&&y>=r.y-p&&y<=r.y+r.h+p;
function circleRectHit(x,y,r,rc){const qx=clamp(x,rc.x,rc.x+rc.w),qy=clamp(y,rc.y,rc.y+rc.h);return(x-qx)**2+(y-qy)**2<r*r}
const normalizeAngle=a=>((a%(Math.PI*2))+Math.PI*2)%(Math.PI*2);
function angleDiff(a,b){let d=Math.abs(normalizeAngle(a)-normalizeAngle(b));return Math.min(d,Math.PI*2-d)}
function weightedChoice(entries){let total=entries.reduce((s,e)=>s+e.weight,0);if(total<=0)return null;let r=Math.random()*total;for(const e of entries){r-=e.weight;if(r<=0)return e.value}return entries.at(-1)?.value||null}
function seeded(seed){let s=seed>>>0;return()=>((s=(s*1664525+1013904223)>>>0)/4294967296)}
function fmtTime(m){m=((Math.floor(m)%1440)+1440)%1440;return`${String(Math.floor(m/60)).padStart(2,'0')}:${String(m%60).padStart(2,'0')}`}
const gameHour=()=>state.minute/60;
const isNight=()=>state.minute>=1200||state.minute<240;
const currentWeather=()=>WEATHER[state.weather]||WEATHER.sunny;
function sceneSize(){if(state.scene==='village')return{w:CONFIG.villageCols*TILE,h:CONFIG.villageRows*TILE};if(state.scene==='shop')return{w:CONFIG.shopCols*TILE,h:CONFIG.shopRows*TILE};return{w:CONFIG.aquariumCols*TILE,h:CONFIG.aquariumRows*TILE}}
function facingVec(){return state.player.dir==='up'?{x:0,y:-1}:state.player.dir==='down'?{x:0,y:1}:state.player.dir==='left'?{x:-1,y:0}:{x:1,y:0}}
const tileRect=o=>rect(o.tx*TILE,o.ty*TILE,o.tw*TILE,o.th*TILE);
function escapeHtml(s){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}

function defaultState(){return{version:CONFIG.saveVersion,absoluteMinutes:480,day:1,minute:480,weather:'sunny',money:0,pendingRevenue:0,lastSettlement:null,scene:'village',player:{x:7*TILE,y:15*TILE,dir:'right'},inventory:{capacity:9,fish:{},items:{wideBait:0,curiosityBait:0,iceBomb:0}},equipment:{rod:'basic',bobber:'basic',bait:null,active:'iceBomb',ownedRods:{basic:1,wide:0}},codex:{},aquarium:{displayed:{},mainTankCapacity:10,ticketPrice:5,isOpen:false,totalVisitors:0,turnedAway:0,todayVisitors:0,todayTicketRevenue:0,todaySatisfactionSum:0,reputation:0},story:{stage:0,firstTankRepaired:false,localReopening:false,busUnlocked:false},buildInventory:{smallTank:1,rug:1,plant:2},placed:[],stats:{caught:0}}}
let state=defaultState();
function normalizeState(s){const d=defaultState();state={...d,...s,version:CONFIG.saveVersion,player:{...d.player,...s.player},inventory:{...d.inventory,...s.inventory,fish:{...(s.inventory?.fish||{})},items:{...d.inventory.items,...(s.inventory?.items||{})}},equipment:{...d.equipment,...s.equipment,ownedRods:{...d.equipment.ownedRods,...(s.equipment?.ownedRods||{})}},aquarium:{...d.aquarium,...s.aquarium,displayed:{...(s.aquarium?.displayed||{})}},story:{...d.story,...s.story},buildInventory:{...d.buildInventory,...s.buildInventory},placed:Array.isArray(s.placed)?s.placed:[],stats:{...d.stats,...s.stats},codex:{...(s.codex||{})}};if(!Number.isFinite(state.absoluteMinutes))state.absoluteMinutes=(Math.max(1,Number(state.day)||1)-1)*1440+(Number(state.minute)||480);state.day=Math.floor(state.absoluteMinutes/1440)+1;state.minute=((state.absoluteMinutes%1440)+1440)%1440;if(!['village','aquarium','shop'].includes(state.scene))state.scene='village';for(const p of state.placed){if(!p.uid)p.uid=`p${uidCounter++}`;if(!p.fish)p.fish={}}}
function loadGame(){try{const raw=localStorage.getItem(CONFIG.saveKey);if(raw)normalizeState(JSON.parse(raw));else{const old=localStorage.getItem(CONFIG.legacyV3Key);normalizeState(old?JSON.parse(old):defaultState())}}catch(e){console.warn('save load failed',e);normalizeState(defaultState())}if(state.scene!=='village')state.scene='village';if(!isPositionFree(state.player.x,state.player.y,10)){state.player.x=7*TILE;state.player.y=15*TILE}}
function saveGame(){try{localStorage.setItem(CONFIG.saveKey,JSON.stringify(state))}catch(e){console.warn('save failed',e)}}

// --- Larger pond village world ------------------------------------------------
const pond={cx:67*TILE,cy:50*TILE,rx:27*TILE,ry:15*TILE};
const buildings=[
{id:'aquarium',tx:18,ty:8,tw:12,th:8,label:'낡은 아쿠아리움',kind:'aquarium',roof:'#66766d',wall:'#d4bf92',sign:'AQUARIUM'},
{id:'shop',tx:43,ty:10,tw:9,th:7,label:'마르코 상점',kind:'shop',roof:'#a85c55',wall:'#e0bd76',sign:'연못 상점'},
{id:'workshop',tx:66,ty:11,tw:10,th:7,label:'브루노 작업장',kind:'workshop',roof:'#765a48',wall:'#c99a65',sign:'수리 작업장'},
{id:'homeA',tx:88,ty:10,tw:8,th:7,label:'로한의 집',kind:'home',roof:'#855d52',wall:'#dfbc7e',sign:''},
{id:'homeB',tx:98,ty:25,tw:8,th:7,label:'연못마을 주택',kind:'home',roof:'#8e625b',wall:'#d9b67b',sign:''},
{id:'homeC',tx:10,ty:39,tw:8,th:7,label:'연못마을 주택',kind:'home',roof:'#796457',wall:'#dec18a',sign:''},
{id:'homeD',tx:27,ty:52,tw:8,th:7,label:'연못마을 주택',kind:'home',roof:'#8a6556',wall:'#d7b67f',sign:''}
];
const busShelter=rect(4*TILE,11*TILE,5*TILE,2.4*TILE);
const rocks=[rect(36*TILE,55*TILE,1.5*TILE,1.3*TILE),rect(91*TILE,51*TILE,1.7*TILE,1.5*TILE),rect(43*TILE,66*TILE,1.3*TILE,1.2*TILE),rect(79*TILE,67*TILE,1.5*TILE,1.2*TILE),rect(102*TILE,57*TILE,1.5*TILE,1.4*TILE)];
const pathTiles=new Set();
const addPathTile=(x,y)=>pathTiles.add(`${x},${y}`);
function addH(x1,x2,y,w=3){for(let x=Math.min(x1,x2);x<=Math.max(x1,x2);x++)for(let o=-Math.floor(w/2);o<=Math.floor(w/2);o++)addPathTile(x,y+o)}
function addV(y1,y2,x,w=3){for(let y=Math.min(y1,y2);y<=Math.max(y1,y2);y++)for(let o=-Math.floor(w/2);o<=Math.floor(w/2);o++)addPathTile(x+o,y)}
addH(7,22,15,3);addV(15,18,22,3);addH(22,47,18,3);addV(18,20,47,3);addH(47,71,20,3);addH(71,92,20,3);addV(20,28,92,3);addH(92,102,28,3);addV(20,37,60,3);addH(43,60,37,3);addV(37,43,43,3);addH(16,43,43,3);addV(43,56,32,3);addH(32,47,56,3);addV(20,35,80,3);addH(80,95,35,3);

const decoRand=seeded(2219451);
const grassTufts=Array.from({length:520},()=>({x:decoRand()*CONFIG.villageCols*TILE,y:decoRand()*CONFIG.villageRows*TILE,t:decoRand()}));
const flowerPatches=Array.from({length:120},()=>({x:decoRand()*CONFIG.villageCols*TILE,y:(6+decoRand()*(CONFIG.villageRows-8))*TILE,c:decoRand()>.5?'#f2b7c7':'#d9b1e5'}));
const trees=Array.from({length:62},(_,i)=>({x:(3+decoRand()*(CONFIG.villageCols-6))*TILE,y:(5+decoRand()*(CONFIG.villageRows-7))*TILE,blossom:i%3!==0,s:.85+(i%5)*.05})).filter(t=>pondNormRaw(t.x,t.y)>1.12&&!buildings.some(b=>rectContains(tileRect(b),t.x,t.y,50))&&!pathTiles.has(`${Math.floor(t.x/TILE)},${Math.floor(t.y/TILE)}`));
const waterSparkles=Array.from({length:86},()=>({a:decoRand()*Math.PI*2,r:.10+decoRand()*.84,p:decoRand()*Math.PI*2}));
function pondBoundaryScale(a){return 1+.075*Math.sin(a*3+.55)+.048*Math.sin(a*5-1.15)+.032*Math.sin(a*9+.9)+.018*Math.sin(a*13-2.2)}
function pondNormRaw(x,y){const nx=(x-pond.cx)/pond.rx,ny=(y-pond.cy)/pond.ry,a=Math.atan2(ny,nx),base=Math.hypot(nx,ny);return base/pondBoundaryScale(a)}
const pondNorm=pondNormRaw;
function getWaterDepth(x,y){if(state.scene!=='village')return null;const n=pondNorm(x,y);if(n>1)return null;if(n>=.72)return'near';if(n>=.42)return'mid';return'far'}
const shoreRand=seeded(913731);
const shoreReeds=Array.from({length:58},()=>{const a=shoreRand()*Math.PI*2,sc=pondBoundaryScale(a)*(.94+shoreRand()*.045);return{x:pond.cx+Math.cos(a)*pond.rx*sc,y:pond.cy+Math.sin(a)*pond.ry*sc,h:8+shoreRand()*12}});
const shoreStones=Array.from({length:31},()=>{const a=shoreRand()*Math.PI*2,sc=pondBoundaryScale(a)*(1.005+shoreRand()*.035);return{x:pond.cx+Math.cos(a)*pond.rx*sc,y:pond.cy+Math.sin(a)*pond.ry*sc,s:5+shoreRand()*9}});
const lilyPads=Array.from({length:23},()=>{const a=shoreRand()*Math.PI*2,rr=.15+shoreRand()*.78,sc=pondBoundaryScale(a)*rr;return{x:pond.cx+Math.cos(a)*pond.rx*sc,y:pond.cy+Math.sin(a)*pond.ry*sc,s:5+shoreRand()*7,flower:shoreRand()>.72}});
const pondRipples=Array.from({length:13},()=>{const a=shoreRand()*Math.PI*2,rr=.15+shoreRand()*.72,sc=pondBoundaryScale(a)*rr;return{x:pond.cx+Math.cos(a)*pond.rx*sc,y:pond.cy+Math.sin(a)*pond.ry*sc,p:shoreRand()*Math.PI*2}});
function playerTouchesPond(x,y,r=18){const d=r*.72;return [[0,0],[r,0],[-r,0],[0,r],[0,-r],[d,d],[d,-d],[-d,d],[-d,-d]].some(([ox,oy])=>pondNorm(x+ox,y+oy)<1.018)}
function isSolidVillage(x,y,r=10){const z=sceneSize();if(x-r<0||y-r<0||x+r>z.w||y+r>z.h)return true;if(playerTouchesPond(x,y,Math.max(17,r)))return true;if(buildings.some(b=>circleRectHit(x,y,r,tileRect(b))))return true;if(rocks.some(o=>circleRectHit(x,y,r,o)))return true;if(circleRectHit(x,y,r,busShelter))return true;return false}

// --- Aquarium interior --------------------------------------------------------
const aquariumTank=rect(9*TILE,5*TILE,15*TILE,5*TILE),managerRoom=rect(46*TILE,4*TILE,13*TILE,10*TILE),managerDesk=rect(50*TILE,7*TILE,3*TILE,2*TILE),aquariumExit=rect(4*TILE,27*TILE,3*TILE,2*TILE),managerDoor=rect(50*TILE,13.58*TILE,3*TILE,.42*TILE),managerWalls=[rect(46*TILE,4*TILE,13*TILE,.42*TILE),rect(46*TILE,4*TILE,.42*TILE,10*TILE),rect(58.58*TILE,4*TILE,.42*TILE,10*TILE),rect(46*TILE,13.58*TILE,4*TILE,.42*TILE),rect(53*TILE,13.58*TILE,6*TILE,.42*TILE)];
function placedRect(p){const d=BUILD_DEFS[p.type];if(!d)return rect(0,0,0,0);const swap=(p.rot||0)%2===1;return rect(p.tx*TILE,p.ty*TILE,(swap?d.th:d.tw)*TILE,(swap?d.tw:d.th)*TILE)}
function isSolidAquarium(x,y,r=10){const s=sceneSize();if(x-r<2*TILE||y-r<2*TILE||x+r>s.w-2*TILE||y+r>s.h-2*TILE)return true;if(circleRectHit(x,y,r,aquariumTank)||circleRectHit(x,y,r,managerDesk))return true;if(managerWalls.some(w=>circleRectHit(x,y,r,w)))return true;for(const p of state.placed){if(BUILD_DEFS[p.type]?.solid&&circleRectHit(x,y,r,placedRect(p)))return true}return false}
function canPlace(type,tx,ty,rot=0){if(state.scene!=='aquarium')return false;const d=BUILD_DEFS[type];if(!d)return false;const swap=rot%2===1,rc=rect(tx*TILE,ty*TILE,(swap?d.th:d.tw)*TILE,(swap?d.tw:d.th)*TILE),bounds=rect(2*TILE,2*TILE,(CONFIG.aquariumCols-4)*TILE,(CONFIG.aquariumRows-4)*TILE);if(rc.x<bounds.x||rc.y<bounds.y||rc.x+rc.w>bounds.x+bounds.w||rc.y+rc.h>bounds.y+bounds.h)return false;if([aquariumTank,managerDesk,...managerWalls,aquariumExit].some(f=>rectHit(rc,f)))return false;if(state.placed.some(p=>rectHit(rc,placedRect(p))))return false;if(rectContains(rc,state.player.x,state.player.y,8))return false;return true}

// --- Marco's actual shop interior --------------------------------------------
const shopCounter=rect(8*TILE,7*TILE,18*TILE,2*TILE),shopServiceZone=rect(9*TILE,9*TILE,16*TILE,2.35*TILE),shopExit=rect(15*TILE,21*TILE,4*TILE,2*TILE);
const shopShelves=[rect(4*TILE,4*TILE,3*TILE,9*TILE),rect(27*TILE,4*TILE,3*TILE,9*TILE),rect(5*TILE,15*TILE,7*TILE,2*TILE),rect(22*TILE,15*TILE,7*TILE,2*TILE)];
const shopMarco={id:'marco',name:'마르코',x:17*TILE,y:6*TILE,dir:'down',style:{hair:'#4a392c',body:'#4f7568',accent:'#d4b865',skin:'#d5a374'}};
function isSolidShop(x,y,r=10){const s=sceneSize();if(x-r<2*TILE||y-r<2*TILE||x+r>s.w-2*TILE||y+r>s.h-2*TILE)return true;if(circleRectHit(x,y,r,shopCounter))return true;if(shopShelves.some(a=>circleRectHit(x,y,r,a)))return true;return false}
function isPositionFree(x,y,r=10){return state.scene==='village'?!isSolidVillage(x,y,r):state.scene==='shop'?!isSolidShop(x,y,r):!isSolidAquarium(x,y,r)}

// --- Village NPCs (Marco is intentionally NOT outside anymore) ----------------
const npcDefs=[
{id:'rohan',name:'로한',x:39*TILE,y:48*TILE,style:{hair:'#3a332e',body:'#315863',accent:'#31444b',skin:'#d5a37b'},speed:46},
{id:'yotri',name:'요트리',x:91*TILE,y:23*TILE,style:{hair:'#453553',body:'#7b638e',accent:'#d8a8c4',skin:'#e1b08e'},speed:50},
{id:'luka',name:'루카',x:10*TILE,y:15*TILE,style:{hair:'#6b4a2f',body:'#4d78a3',accent:'#efd185',skin:'#e3ad80'},speed:53},
{id:'bruno',name:'브루노',x:72*TILE,y:20*TILE,style:{hair:'#57443a',body:'#846541',accent:'#9ca29d',skin:'#d1a078'},speed:44},
{id:'resident1',name:'주민',x:36*TILE,y:22*TILE,style:{hair:'#503a35',body:'#6c8872',accent:'#d6bd75',skin:'#daa87c'},speed:42},
{id:'resident2',name:'주민',x:93*TILE,y:37*TILE,style:{hair:'#3c3b45',body:'#9b715e',accent:'#d7a8a2',skin:'#dba67d'},speed:46},
{id:'resident3',name:'주민',x:38*TILE,y:58*TILE,style:{hair:'#5d4939',body:'#7891a5',accent:'#c7b56d',skin:'#d9a57d'},speed:44}
],npcs=npcDefs.map(d=>({...d,path:[],pathIndex:0,repath:Math.random()*2,dir:'down',moving:false}));
const tileKey=(x,y)=>`${x},${y}`;
function npcBlocked(tx,ty){if(tx<0||ty<0||tx>=CONFIG.villageCols||ty>=CONFIG.villageRows)return true;const x=(tx+.5)*TILE,y=(ty+.5)*TILE;if(pondNorm(x,y)<1.03)return true;if(buildings.some(b=>rectContains(tileRect(b),x,y)))return true;if(rocks.some(r=>rectContains(r,x,y)))return true;if(rectContains(busShelter,x,y))return true;return false}
function findPath(sx,sy,ex,ey,max=2600){const start={x:Math.floor(sx/TILE),y:Math.floor(sy/TILE)},end={x:Math.floor(ex/TILE),y:Math.floor(ey/TILE)};if(npcBlocked(end.x,end.y))return[];const open=[{...start,g:0,f:Math.abs(end.x-start.x)+Math.abs(end.y-start.y)}],came=new Map(),score=new Map([[tileKey(start.x,start.y),0]]);let visited=0;while(open.length&&visited++<max){open.sort((a,b)=>a.f-b.f);const c=open.shift();if(c.x===end.x&&c.y===end.y){const out=[];let n={x:end.x,y:end.y};while(n.x!==start.x||n.y!==start.y){out.push({x:(n.x+.5)*TILE,y:(n.y+.5)*TILE});const p=came.get(tileKey(n.x,n.y));if(!p)break;n=p}return out.reverse()}for(const[dx,dy]of[[1,0],[-1,0],[0,1],[0,-1]]){const nx=c.x+dx,ny=c.y+dy;if(npcBlocked(nx,ny))continue;const ng=c.g+1,k=tileKey(nx,ny);if(ng>=(score.get(k)??Infinity))continue;score.set(k,ng);came.set(k,{x:c.x,y:c.y});open.push({x:nx,y:ny,g:ng,f:ng+Math.abs(end.x-nx)+Math.abs(end.y-ny)})}}return[]}
function npcTarget(n){const h=gameHour();switch(n.id){case'rohan':return h>=6&&h<19?{x:40*TILE,y:48*TILE}:{x:92*TILE,y:20*TILE};case'yotri':return isNight()?{x:93*TILE,y:45*TILE}:{x:91*TILE,y:23*TILE};case'luka':return h>=7&&h<18?{x:10*TILE,y:15*TILE}:{x:91*TILE,y:20*TILE};case'bruno':return h>=6&&h<20?{x:72*TILE,y:20*TILE}:{x:78*TILE,y:23*TILE};case'resident1':return h<12?{x:36*TILE,y:22*TILE}:{x:48*TILE,y:36*TILE};case'resident2':return h<16?{x:93*TILE,y:37*TILE}:{x:98*TILE,y:40*TILE};case'resident3':return h<15?{x:38*TILE,y:58*TILE}:{x:38*TILE,y:43*TILE};default:return{x:n.x,y:n.y}}}
function updateNpcs(dt){if(state.scene!=='village'||ui.modal||fishing)return;for(const n of npcs){n.repath-=dt;if(n.repath<=0){n.repath=4+Math.random()*2;const t=npcTarget(n);if(t&&Number.isFinite(t.x)&&Number.isFinite(t.y)&&dist(n.x,n.y,t.x,t.y)>22){n.path=findPath(n.x,n.y,t.x,t.y);n.pathIndex=0}}const p=n.path[n.pathIndex];if(!p){n.moving=false;continue}const dx=p.x-n.x,dy=p.y-n.y,d=Math.hypot(dx,dy);if(d<3){n.pathIndex++;n.moving=false;continue}n.moving=true;if(Math.abs(dx)>Math.abs(dy))n.dir=dx<0?'left':'right';else n.dir=dy<0?'up':'down';const sp=n.speed*dt;n.x+=dx/d*sp;n.y+=dy/d*sp}}

// --- Economy / inventory / time ----------------------------------------------
const fishCount=id=>state.inventory.fish[id]||0,itemCount=id=>state.inventory.items[id]||0;
const uniqueStacks=()=>Object.values(state.inventory.fish).filter(v=>v>0).length+Object.values(state.inventory.items).filter(v=>v>0).length;
function bagSpaceFor(kind,id){const g=kind==='fish'?state.inventory.fish:state.inventory.items;return(g[id]||0)>0||uniqueStacks()<state.inventory.capacity}
function addFish(id,q=1){if(!bagSpaceFor('fish',id))return false;state.inventory.fish[id]=(state.inventory.fish[id]||0)+q;return true}
function removeFish(id,q=1){if(fishCount(id)<q)return false;state.inventory.fish[id]-=q;if(state.inventory.fish[id]<=0)delete state.inventory.fish[id];return true}
function addItem(id,q=1){if(!bagSpaceFor('item',id))return false;state.inventory.items[id]=clamp((state.inventory.items[id]||0)+q,0,ITEMS[id]?.stack||999);return true}
function removeItem(id,q=1){if(itemCount(id)<q)return false;state.inventory.items[id]-=q;if(state.inventory.items[id]<=0)state.inventory.items[id]=0;return true}
function fishSize(id){const[a,b]=SPECIES[id].size,r=(Math.random()+Math.random()+Math.random())/3;return Math.round((a+(b-a)*r)*10)/10}
function recordCatch(id,size){const rec=state.codex[id]||{count:0,minSize:null,maxSize:null,firstLocation:'연못 마을',firstDay:state.day},record=rec.maxSize==null||size>rec.maxSize;rec.count++;rec.minSize=rec.minSize==null?size:Math.min(rec.minSize,size);rec.maxSize=rec.maxSize==null?size:Math.max(rec.maxSize,size);rec.lastDay=state.day;state.codex[id]=rec;state.stats.caught++;return record}
const displayedTotal=obj=>Object.values(obj).reduce((a,b)=>a+(Number(b)||0),0);
function reputation(){let r=0;const groups=[state.aquarium.displayed,...state.placed.filter(p=>BUILD_DEFS[p.type]?.capacity).map(p=>p.fish||{})];for(const g of groups)for(const[id,q]of Object.entries(g)){const s=SPECIES[id];if(s)r+=(s.rarity==='고급'?2:1)*q}return r}
const chooseWeather=()=>weightedChoice(Object.values(WEATHER).map(w=>({weight:w.weight,value:w.id})))||'sunny';
const recommendedTicket=()=>clamp(3+Math.floor(reputation()/3),3,25);
const acceptance=()=>clamp(.93-Math.max(0,state.aquarium.ticketPrice-recommendedTicket())*.085,.12,.96);
function runSettlement(){const a=state.aquarium,vis=a.todayVisitors,rev=a.todayTicketRevenue,avg=vis?Math.round(a.todaySatisfactionSum/vis):0;state.money+=rev;state.pendingRevenue=Math.max(0,state.pendingRevenue-rev);state.lastSettlement={day:Math.max(1,state.day-1),visitors:vis,ticketPrice:a.ticketPrice,revenue:rev,expenses:0,net:rev,avgSatisfaction:avg};a.todayVisitors=0;a.todayTicketRevenue=0;a.todaySatisfactionSum=0;if(vis||rev)openSettlement(state.lastSettlement)}
function setAbsoluteMinutes(newAbs){const old=state.absoluteMinutes,oldIndex=Math.floor((old-240)/1440),newIndex=Math.floor((newAbs-240)/1440);state.absoluteMinutes=newAbs;state.day=Math.floor(newAbs/1440)+1;state.minute=((newAbs%1440)+1440)%1440;if(newIndex>oldIndex){runSettlement();state.weather=chooseWeather();showToast(`새로운 아침 · ${currentWeather().icon} ${currentWeather().name}`)}}
const advanceTime=realSec=>setAbsoluteMinutes(state.absoluteMinutes+realSec*CONFIG.gameMinutesPerRealSecond);
function skipToHour(hour){const cur=state.absoluteMinutes;let target=Math.floor(cur/1440)*1440+hour*60;if(target<=cur)target+=1440;setAbsoluteMinutes(target);saveGame()}
const aquariumOpenHours=()=>state.minute>=420&&state.minute<1140;
const visualVisitors=[];
function updateVisitors(dt){if(!state.aquarium.isOpen||reputation()<=0||!aquariumOpenHours())return;visitorClock-=dt;if(visitorClock<=0){visitorClock=clamp(8-reputation()*.14,2.7,8)+Math.random()*3;const a=state.aquarium,ok=Math.random()<acceptance();if(ok){a.totalVisitors++;a.todayVisitors++;a.todayTicketRevenue+=a.ticketPrice;a.todaySatisfactionSum+=clamp(82-(a.ticketPrice-recommendedTicket())*6+Math.random()*12-6,25,100);state.pendingRevenue+=a.ticketPrice;if(state.scene==='aquarium')visualVisitors.push({x:6*TILE,y:27*TILE,tx:(12+Math.random()*18)*TILE,ty:(14+Math.random()*9)*TILE,t:8+Math.random()*8,shirt:['#d98b70','#6e9dbc','#8e78a4','#75a07e'][Math.floor(Math.random()*4)],dir:'up'})}else a.turnedAway++}for(let i=visualVisitors.length-1;i>=0;i--){const v=visualVisitors[i];v.t-=dt;const dx=v.tx-v.x,dy=v.ty-v.y,d=Math.hypot(dx,dy);if(d>4){if(Math.abs(dx)>Math.abs(dy))v.dir=dx<0?'left':'right';else v.dir=dy<0?'up':'down';v.x+=dx/d*40*dt;v.y+=dy/d*40*dt}if(v.t<=0)visualVisitors.splice(i,1)}}

// --- Fishing -----------------------------------------------------------------
function canStartCast(){if(state.scene!=='village'||ui.modal||fishing||buildState.active)return false;const f=facingVec();for(let d=22;d<=126;d+=6)if(getWaterDepth(state.player.x+f.x*d,state.player.y+f.y*d))return true;return false}
function startCast(){if(!canStartCast())return false;cast={mode:'charging',value:0,dir:1,landing:null,depth:null,biteTimer:0,pulse:0};dom.castGauge.classList.remove('hidden');return true}
function updateCast(dt){if(cast.mode==='charging'){cast.value+=cast.dir*92*dt;if(cast.value>=100){cast.value=100;cast.dir=-1}else if(cast.value<=0){cast.value=0;cast.dir=1}const f=facingVec(),d=lerp(CONFIG.castMinDistance,CONFIG.castMaxDistance,cast.value/100);cast.landing={x:state.player.x+f.x*d,y:state.player.y+f.y*d};cast.depth=getWaterDepth(cast.landing.x,cast.landing.y);dom.castFill.style.width=`${cast.value}%`;dom.castMarker.style.left=`calc(${cast.value}% - 2px)`;dom.castDepthHint.textContent=cast.depth?`${DEPTH[cast.depth].label} · ${DEPTH[cast.depth].name}`:'이 거리에는 물이 없음'}else if(cast.mode==='waiting'){cast.pulse+=dt*4;cast.biteTimer-=dt;if(cast.biteTimer<=0){const s=chooseBite(cast.depth);if(!s){showToast('지금 이 수심에는 입질이 거의 없네요.');resetCast();return}startFishing(s)}}}
function releaseCast(){if(cast.mode!=='charging')return;dom.castGauge.classList.add('hidden');if(!cast.landing||!cast.depth){showToast('찌가 물에 닿지 않았어요.');resetCast();return}cast.mode='waiting';cast.biteTimer=lerp(CONFIG.biteMin,CONFIG.biteMax,Math.random())/(BOBBERS[state.equipment.bobber]?.biteMult||1);if(state.equipment.bait&&itemCount(state.equipment.bait)>0){removeItem(state.equipment.bait,1);if(itemCount(state.equipment.bait)<=0)state.equipment.bait=null}showToast(`${DEPTH[cast.depth].label}에 찌가 떨어졌습니다.`);saveGame()}
function resetCast(){cast={mode:'idle',value:0,dir:1,landing:null,depth:null,biteTimer:0,pulse:0};dom.castGauge.classList.add('hidden')}
function speciesEligible(s,depth){let depthOk=s.depths.includes(depth);if(s.id==='loach'&&depth==='far')depthOk=state.weather==='rain';if(!depthOk||!s.weather.includes(state.weather))return false;if(s.time==='night'&&!isNight())return false;return true}
function chooseBite(depth){const entries=[];for(const s of Object.values(SPECIES)){if(!speciesEligible(s,depth))continue;let w=s.weight;if(state.equipment.bait==='curiosityBait'&&s.rarity==='고급')w*=1.65;entries.push({weight:w,value:s})}return weightedChoice(entries)}
function pattern(s){const base={speed:.58+s.difficulty*.1,turn:1.1+s.difficulty*.18,burst:0,rest:0};switch(s.pattern){case'minnow':return{...base,speed:.72,turn:2.1};case'crucian':return{...base,speed:.56,turn:.82};case'loach':return{...base,speed:.58,turn:1.15,burst:.38,rest:1};case'snail':return{...base,speed:.14,turn:.24};case'waterBug':return{...base,speed:.54,turn:1.25,burst:.5};case'frog':return{...base,speed:.44,turn:.7,burst:1.1,rest:.85};case'carp':return{...base,speed:.61,turn:.74,burst:.72};case'shrimp':return{...base,speed:.54,turn:1.1,burst:1.08};case'beetle':return{...base,speed:.7,turn:1.3,rest:.4};case'goldfish':return{...base,speed:.64,turn:1.68};case'salamander':return{...base,speed:.56,turn:.98,burst:1.02};case'softshell':return{...base,speed:.36,turn:.5,burst:.68};case'snakehead':return{...base,speed:.5,turn:.65,burst:1.5};case'bullhead':return{...base,speed:.46,turn:.75,burst:1.38};default:return base}}
function startFishing(s){
  const rod=RODS[state.equipment.rod]?.barMult||1,bait=state.equipment.bait==='wideBait'?1.2:1;
  const legacyZone=s.difficulty<=1?.60:s.difficulty===2?.58:.56;
  const base=pattern(s).speed;
  fishing={species:s,barAngle:0,barSpeed:-BAR_ACCEL.releaseStartSpeed,fishAngle:Math.random()*Math.PI*2,fishVel:(Math.random()<.5?-1:1)*base,fishTarget:0,gauge:50,zone:clamp(legacyZone*rod*bait,.42,1.20),ice:0,result:null,resultTimer:0};
  fishing.fishTarget=fishing.fishVel;
  cast.mode='bite';dom.fishingSpecies.textContent=s.name;dom.fishingDifficulty.textContent=`난이도 ${s.difficulty}`;dom.iceBombCount.textContent=`×${itemCount('iceBomb')}`;canvas.style.filter='blur(5px) brightness(.72)';dom.fishingOverlay.classList.remove('hidden')
}
function updateFishing(dt){
  if(!fishing)return;if(fishing.result){fishing.resultTimer-=dt;if(fishing.resultTimer<=0)finishFishing();return}
  const hold=input.pointerDown;
  if(hold){
    if(fishing.barSpeed<0)fishing.barSpeed=Math.min(0,fishing.barSpeed+BAR_ACCEL.pressTurnAccel*dt);
    else if(fishing.barSpeed<BAR_ACCEL.pressStartSpeed)fishing.barSpeed=Math.min(BAR_ACCEL.pressStartSpeed,fishing.barSpeed+BAR_ACCEL.pressTurnAccel*dt);
    else fishing.barSpeed=Math.min(BAR_ACCEL.maxSpeed,fishing.barSpeed+BAR_ACCEL.pressHoldAccel*dt);
  }else{
    if(fishing.barSpeed>0)fishing.barSpeed=Math.max(0,fishing.barSpeed-BAR_ACCEL.releaseTurnAccel*dt);
    else if(fishing.barSpeed>-BAR_ACCEL.releaseStartSpeed)fishing.barSpeed=Math.max(-BAR_ACCEL.releaseStartSpeed,fishing.barSpeed-BAR_ACCEL.releaseTurnAccel*dt);
    else fishing.barSpeed=Math.max(-BAR_ACCEL.maxSpeed,fishing.barSpeed-BAR_ACCEL.releaseHoldAccel*dt);
  }
  fishing.barAngle=normalizeAngle(fishing.barAngle+fishing.barSpeed*dt);
  const frames=dt*60;
  const reverseChance=1-Math.pow(1-.010,frames),varyChance=1-Math.pow(1-.013,frames);
  if(Math.random()<reverseChance)fishing.fishTarget*=-1;
  if(Math.random()<varyChance){const base=Math.max(.006*60,Math.abs(fishing.fishTarget));const varied=Math.min(.033*60,Math.max(.006*60,base+(Math.random()-.5)*(.010*60)));fishing.fishTarget=Math.sign(fishing.fishTarget||1)*varied}
  fishing.fishTarget=clamp(fishing.fishTarget,-.033*60,.033*60);
  const inertia=1-Math.pow(1-.085,frames);fishing.fishVel+=(fishing.fishTarget-fishing.fishVel)*inertia;
  const slow=fishing.ice>0?.32:1;if(fishing.ice>0)fishing.ice=Math.max(0,fishing.ice-dt);
  fishing.fishAngle=normalizeAngle(fishing.fishAngle+fishing.fishVel*slow*dt);
  const inside=angleDiff(fishing.barAngle+fishing.zone/2,fishing.fishAngle)<fishing.zone/2;
  fishing.gauge=clamp(fishing.gauge+(inside?19.2:-9.6)*dt,0,100);dom.catchFill.style.width=`${fishing.gauge}%`;
  if(fishing.gauge>=100)resolveFishing(true);else if(fishing.gauge<=0)resolveFishing(false)
}
function useIceBomb(){if(!fishing||fishing.ice>0||itemCount('iceBomb')<=0)return;removeItem('iceBomb',1);fishing.ice=3;dom.iceBombCount.textContent=`×${itemCount('iceBomb')}`;saveGame()}
function resolveFishing(ok){if(!fishing||fishing.result)return;const s=fishing.species;if(ok){const size=fishSize(s.id);if(addFish(s.id,1)){const rec=recordCatch(s.id,size);fishing.result={ok:true,size,record:rec};if(state.story.stage===1)state.story.stage=2;showToast(rec?`★ 신기록! ${s.name} ${size.toFixed(1)}cm`:`잡았다! ${s.name} ${size.toFixed(1)}cm`)}else{fishing.result={ok:false,full:true};showToast('가방이 가득 찼습니다.')}}else{fishing.result={ok:false};showToast('놓쳤다!')}fishing.resultTimer=1.15;saveGame();refreshHud(true)}
function finishFishing(){dom.fishingOverlay.classList.add('hidden');canvas.style.filter='';fishing=null;input.pointerDown=false;resetCast()}

// --- Movement & interactions --------------------------------------------------
function updatePlayer(dt){if(ui.modal||fishing||cast.mode==='charging'||cast.mode==='waiting'||buildState.active)return;let dx=0,dy=0;if(input.keys.has('KeyW'))dy--;if(input.keys.has('KeyS'))dy++;if(input.keys.has('KeyA'))dx--;if(input.keys.has('KeyD'))dx++;if(!dx&&!dy)return;const l=Math.hypot(dx,dy);dx/=l;dy/=l;if(Math.abs(dx)>Math.abs(dy))state.player.dir=dx<0?'left':'right';else state.player.dir=dy<0?'up':'down';const step=CONFIG.playerSpeed*dt,nx=state.player.x+dx*step,ny=state.player.y+dy*step;if(isPositionFree(nx,state.player.y,11))state.player.x=nx;if(isPositionFree(state.player.x,ny,11))state.player.y=ny}
function pointerWorld(e){const r=canvas.getBoundingClientRect();input.sx=(e.clientX-r.left)*(canvas.width/r.width);input.sy=(e.clientY-r.top)*(canvas.height/r.height);input.wx=input.sx+camera.x;input.wy=input.sy+camera.y;if(buildState.active){buildState.tx=Math.floor(input.wx/TILE);buildState.ty=Math.floor(input.wy/TILE);buildState.valid=canPlace(buildState.type,buildState.tx,buildState.ty,buildState.rot);updateBuildBar()}}
function nearestVillageNpc(x,y){if(state.scene!=='village')return null;let best=null,bd=CONFIG.interactDistance;for(const n of npcs){const dp=dist(state.player.x,state.player.y,n.x,n.y),dc=dist(x,y,n.x,n.y);if(dp<bd&&dc<72){best=n;bd=dp}}return best}
function buildingAt(x,y){if(state.scene!=='village')return null;for(const b of buildings){const rc=tileRect(b),qx=clamp(state.player.x,rc.x,rc.x+rc.w),qy=clamp(state.player.y,rc.y,rc.y+rc.h);if(rectContains(rc,x,y,14)&&dist(state.player.x,state.player.y,qx,qy)<CONFIG.interactDistance)return b}return null}
function shopObjectAt(x,y){if(state.scene!=='shop')return null;const customerSide=rectContains(shopServiceZone,state.player.x,state.player.y,18);const clickedCounter=rectContains(shopCounter,x,y,24);const clickedMarco=dist(x,y,shopMarco.x,shopMarco.y)<92;if(customerSide&&(clickedCounter||clickedMarco))return{id:'shopService'};if(rectContains(shopExit,x,y,12)&&dist(state.player.x,state.player.y,17*TILE,21*TILE)<130)return{id:'shopExit'};return null}
function aquariumObjectAt(x,y){if(state.scene!=='aquarium')return null;if(rectContains(aquariumExit,x,y,12)&&dist(state.player.x,state.player.y,5.5*TILE,28*TILE)<150)return{id:'exit'};if(rectContains(aquariumTank,x,y,12)){const qx=clamp(state.player.x,aquariumTank.x,aquariumTank.x+aquariumTank.w),qy=clamp(state.player.y,aquariumTank.y,aquariumTank.y+aquariumTank.h);if(dist(state.player.x,state.player.y,qx,qy)<CONFIG.interactDistance)return{id:'mainTank'}}if(rectContains(managerDesk,x,y,10)&&rectContains(managerRoom,state.player.x,state.player.y)){const qx=clamp(state.player.x,managerDesk.x,managerDesk.x+managerDesk.w),qy=clamp(state.player.y,managerDesk.y,managerDesk.y+managerDesk.h);if(dist(state.player.x,state.player.y,qx,qy)<CONFIG.interactDistance)return{id:'manager'}}for(const p of state.placed){const rc=placedRect(p),qx=clamp(state.player.x,rc.x,rc.x+rc.w),qy=clamp(state.player.y,rc.y,rc.y+rc.h);if(rectContains(rc,x,y,8)&&dist(state.player.x,state.player.y,qx,qy)<CONFIG.interactDistance)return{id:'placed',p}}return null}
function targetAtPointer(){return nearestVillageNpc(input.wx,input.wy)||buildingAt(input.wx,input.wy)||shopObjectAt(input.wx,input.wy)||aquariumObjectAt(input.wx,input.wy)}
function handleWorldClick(){const t=targetAtPointer();if(!t){showToast('조금 더 가까이 가서 좌클릭해보세요.');return}if(t.name&&t.style){interactNpc(t);return}if(t.kind){interactBuilding(t);return}if(t.id==='shopService'){openShop();return}if(t.id==='shopExit'){transitionScene('village','shop');return}if(t.id==='exit'){transitionScene('village','aquarium');return}if(t.id==='mainTank'){openMainTank();return}if(t.id==='manager'){openManager();return}if(t.id==='placed'){if(BUILD_DEFS[t.p.type]?.capacity)openPlacedTank(t.p.uid);else showToast(BUILD_DEFS[t.p.type]?.name||'배치물')}}
function interactBuilding(b){if(b.kind==='aquarium')transitionScene('aquarium','village');else if(b.kind==='shop')transitionScene('shop','village');else if(b.kind==='workshop')interactNpc(npcs.find(n=>n.id==='bruno'));else showDialogue('마을 주민의 집','작고 따뜻한 연못마을의 집이다. 창가에 봄꽃이 놓여 있다.','🏠')}
function interactNpc(n){switch(n.id){case'bruno':if(!state.story.firstTankRepaired){state.story.firstTankRepaired=true;state.story.stage=Math.max(state.story.stage,1);showDialogue('브루노','낡긴 했지만 메인 수조 하나는 다시 살릴 수 있겠어. 여과장치는 손봤다. 이제 연못에서 뭔가 잡아 와봐.','🔧');saveGame()}else showDialogue('브루노','수조는 내가 살려놨다. 안에 뭘 넣을지는 네 몫이야.','🔧');break;case'rohan':showDialogue('로한','연못은 물색부터 봐. 가장자리는 얕고, 짙은 쪽으로 갈수록 깊다. 어느 수심에 찌를 넣느냐에 따라 사는 녀석이 달라.','🎣');break;case'yotri':showDialogue('요트리',isNight()?'밤 연못은 낮이랑 달라. 조용히 보면 밤에만 움직이는 애들이 보여.':'난 밤에 연못 보는 걸 좋아해. 낮에는 안 보이던 애들이 나오거든.','🌙');break;case'luka':showDialogue('루카','저 버스, 오늘도 그냥 지나가네. 언젠가는 여기에도 다시 멈추겠지?','🚌');break;default:showDialogue('마을 주민','아쿠아리움 다시 연다며? 마을이 조금 활기차겠네.','🙂')}}
function transitionScene(scene,from){dom.sceneFade.classList.add('active');setTimeout(()=>{state.scene=scene;resetCast();input.keys.clear();input.pointerDown=false;buildState.active=false;dom.buildBar.classList.add('hidden');if(scene==='aquarium'){state.player.x=6*TILE;state.player.y=27*TILE;state.player.dir='up'}else if(scene==='shop'){state.player.x=17*TILE;state.player.y=20*TILE;state.player.dir='up'}else if(from==='shop'){const b=buildings.find(x=>x.kind==='shop');state.player.x=(b.tx+b.tw/2)*TILE;state.player.y=(b.ty+b.th+1.2)*TILE;state.player.dir='down'}else{const b=buildings.find(x=>x.kind==='aquarium');state.player.x=(b.tx+b.tw/2)*TILE;state.player.y=(b.ty+b.th+1.2)*TILE;state.player.dir='down'}camera.x=camera.y=0;saveGame();refreshHud(true);setTimeout(()=>dom.sceneFade.classList.remove('active'),70)},170)}

// --- Build system -------------------------------------------------------------
function openBuildMenu(){if(state.scene!=='aquarium'){showToast('배치는 아쿠아리움 안에서 사용할 수 있어요.');return}const cards=Object.values(BUILD_DEFS).map(d=>`<div class="build-item"><b>${d.icon} ${d.name}</b><div>${d.tw}×${d.th} 타일${d.rotatable?' · 회전 가능':''}</div><div>보유 ${state.buildInventory[d.id]||0}개</div><button data-action="place" data-id="${d.id}" ${(state.buildInventory[d.id]||0)<=0?'disabled':''}>배치 시작</button></div>`).join('');openModal('build',`<h2 class="panel-title">🛠 아쿠아리움 배치</h2><div class="panel-sub">배치물은 타일 단위로 놓이고, 플레이어는 자유 이동합니다.</div><div class="build-list">${cards}</div>`)}
function startPlacement(type){closeModal(false);buildState={active:true,type,rot:0,tx:Math.floor(state.player.x/TILE)+2,ty:Math.floor(state.player.y/TILE),valid:false};dom.buildBar.classList.remove('hidden');updateBuildBar()}
function updateBuildBar(){if(!buildState.active)return;const d=BUILD_DEFS[buildState.type];dom.buildName.textContent=`${d.icon} ${d.name} 배치`;buildState.valid=canPlace(buildState.type,buildState.tx,buildState.ty,buildState.rot);dom.buildStatus.textContent=buildState.valid?'좌클릭으로 설치':'이 위치에는 설치할 수 없음';dom.buildStatus.style.color=buildState.valid?'#bfe7ba':'#ffb0a2'}
function confirmPlacement(){if(!buildState.active||!buildState.valid)return;const type=buildState.type;if((state.buildInventory[type]||0)<=0){cancelPlacement();return}state.placed.push({uid:`p${Date.now().toString(36)}${uidCounter++}`,type,tx:buildState.tx,ty:buildState.ty,rot:buildState.rot,fish:{}});state.buildInventory[type]--;showToast(`${BUILD_DEFS[type].name} 설치 완료`);saveGame();cancelPlacement()}
function cancelPlacement(){buildState.active=false;buildState.type=null;dom.buildBar.classList.add('hidden')}
function rotatePlacement(){if(!buildState.active)return;const d=BUILD_DEFS[buildState.type];if(!d.rotatable)return;buildState.rot=(buildState.rot+1)%2;updateBuildBar()}

// --- Modal UI ----------------------------------------------------------------
function showToast(text,d=2.2){dom.toast.textContent=text;dom.toast.classList.remove('hidden');ui.toast=d}
function updateToast(dt){if(ui.toast>0){ui.toast-=dt;if(ui.toast<=0)dom.toast.classList.add('hidden')}}
function openModal(kind,html){ui.modal=kind;dom.modalContent.innerHTML=html;dom.modalLayer.classList.remove('hidden');canvas.style.filter='blur(4px) brightness(.76)';bindModalActions()}
function closeModal(save=true){ui.modal=null;dom.modalLayer.classList.add('hidden');dom.modalContent.innerHTML='';canvas.style.filter='';if(save)saveGame();refreshHud(true)}
function showDialogue(name,text,emoji='🙂'){openModal('dialogue',`<div class="dialogue-portrait">${emoji}</div><div class="dialogue-name">${escapeHtml(name)}</div><div class="dialogue-text">${escapeHtml(text)}</div>`)}
function openInventory(){const slots=[];for(const[id,q]of Object.entries(state.inventory.fish))if(q>0){const s=SPECIES[id];slots.push({icon:s.icon,name:s.name,q,meta:`${s.rarity} · ${s.price}G`})}for(const[id,q]of Object.entries(state.inventory.items))if(q>0){const it=ITEMS[id];slots.push({icon:it.icon,name:it.name,q,meta:it.description})}while(slots.length<state.inventory.capacity)slots.push(null);openModal('inventory',`<h2 class="panel-title">🎒 가방 · ${uniqueStacks()}/${state.inventory.capacity}칸</h2><div class="panel-sub">생물은 같은 종끼리 한 슬롯에 합쳐집니다. 개체 크기는 도감 기록으로 남습니다.</div><div class="inventory-grid">${slots.slice(0,state.inventory.capacity).map((s,i)=>s?`<div class="inventory-slot"><span class="slot-no">${i+1}</span><span class="big-icon">${s.icon}</span><b>${s.name}</b><div class="quantity">×${s.q}</div><small>${s.meta}</small></div>`:`<div class="inventory-slot"><span class="slot-no">${i+1}</span><span class="big-icon" style="opacity:.15">·</span><small>빈 칸</small></div>`).join('')}</div>`)}
function openCodex(){const cards=Object.values(SPECIES).map(s=>{const r=state.codex[s.id],depth=s.id==='loach'?'근 · 비 오는 날 원':s.depths.map(d=>DEPTH[d].name.replace('거리','')).join('/');return`<div class="panel-card codex-card ${r?'':'locked'} ${s.rarity==='고급'?'rarity-advanced':''}"><b>${r?s.icon:'?'} ${r?s.name:'???'}</b>${r?`잡은 횟수 ${r.count}<br>최대 ${r.maxSize?.toFixed(1)??'—'}cm<br>최소 ${r.minSize?.toFixed(1)??'—'}cm<br>수심 ${depth}<br>시간 ${s.time==='night'?'밤 20:00–04:00':'종일'}<br>날씨 ${s.weather.map(w=>WEATHER[w].name).join('/')}`:'아직 발견하지 못했습니다.'}</div>`}).join('');openModal('codex',`<h2 class="panel-title">📖 연못 생물 도감 · ${Object.keys(state.codex).length}/14</h2><div class="panel-sub">크기는 잡을 때마다 측정되지만 가방에 개체별로 저장하지 않습니다.</div><div class="panel-grid">${cards}</div>`)}
function openEquipment(){openModal('equipment',`<h2 class="panel-title">🎣 낚시 장비</h2><div class="equipment-grid"><div class="equip-card"><b>낚싯대</b><strong>${RODS[state.equipment.rod].name}</strong><p>${RODS[state.equipment.rod].description}</p>${state.equipment.ownedRods.wide?`<button class="panel-action" data-action="equipRod" data-id="${state.equipment.rod==='wide'?'basic':'wide'}">교체</button>`:''}</div><div class="equip-card"><b>찌</b><strong>${BOBBERS[state.equipment.bobber].name}</strong><p>${BOBBERS[state.equipment.bobber].description}</p></div><div class="equip-card"><b>미끼</b><strong>${state.equipment.bait?ITEMS[state.equipment.bait].name:'장착 안 함'}</strong><p>와이드 ${itemCount('wideBait')} · 호기심 ${itemCount('curiosityBait')}</p><button class="panel-action" data-action="cycleBait">미끼 변경</button></div><div class="equip-card"><b>액티브</b><strong>얼음 폭탄</strong><p>보유 ${itemCount('iceBomb')}개</p></div></div>`)}
function displayedAllTotal(){let t=displayedTotal(state.aquarium.displayed);for(const p of state.placed)t+=displayedTotal(p.fish||{});return t}
function openAquariumStatus(){const last=state.lastSettlement;openModal('aquarium',`<h2 class="panel-title">🐠 아쿠아리움 상태</h2><div class="panel-grid"><div class="panel-card"><b>운영</b>${state.aquarium.isOpen?'마을 주민 대상 재개장':'아직 폐업 상태'}<br>전시 생물 ${displayedAllTotal()}마리<br>평판 ${reputation()}</div><div class="panel-card"><b>오늘</b>방문 ${state.aquarium.todayVisitors}명<br>정산 대기 ${state.aquarium.todayTicketRevenue}G</div><div class="panel-card"><b>누적</b>입장 ${state.aquarium.totalVisitors}명<br>입장 포기 ${state.aquarium.turnedAway}명</div>${last?`<div class="panel-card"><b>최근 정산</b>${last.day}일 · ${last.visitors}명<br>순수익 ${last.net}G</div>`:''}</div><div class="panel-sub" style="margin-top:12px">입장료 변경과 시간 넘기기는 관리자실 장부에서만 할 수 있습니다.</div>`)}
function openManager(){openModal('manager',`<h2 class="panel-title">📒 관리자실 장부</h2><div class="panel-card"><b>입장료 관리</b><div class="manager-row"><button data-action="ticketMinus">−</button><div class="manager-price">${state.aquarium.ticketPrice}G</div><button data-action="ticketPlus">＋</button></div>추천 ${recommendedTicket()}G · 예상 입장 확률 ${Math.round(acceptance()*100)}%</div><div class="panel-card" style="margin-top:10px"><b>선택적 휴식</b><div class="manager-row"><button class="panel-action gold" data-action="sleep6">다음 06:00까지 쉬기</button><button class="panel-action" data-action="skipNoon">다음 12:00까지 쉬기</button></div></div>`)}
function tankPanelHtml(title,fishMap,capacity,kind,uid=''){const displayed=Object.entries(fishMap).filter(([,q])=>q>0).map(([id,q])=>`<div class="panel-card"><b>${SPECIES[id].icon} ${SPECIES[id].name} ×${q}</b><button data-action="tankRemove" data-id="${id}" data-kind="${kind}" data-uid="${uid}">1마리 꺼내기</button></div>`).join(''),available=Object.entries(state.inventory.fish).filter(([,q])=>q>0).map(([id,q])=>`<div class="panel-card"><b>${SPECIES[id].icon} ${SPECIES[id].name} ×${q}</b><button data-action="tankAdd" data-id="${id}" data-kind="${kind}" data-uid="${uid}" ${displayedTotal(fishMap)>=capacity?'disabled':''}>1마리 전시</button></div>`).join('');return`<h2 class="panel-title">🐠 ${title} · ${displayedTotal(fishMap)}/${capacity}</h2><div class="panel-grid">${displayed||'<div class="panel-card">아직 비어 있습니다.</div>'}</div><h3>가방에서 넣기</h3><div class="panel-grid">${available||'<div class="panel-card">전시할 생물이 없습니다.</div>'}</div>`}
function openMainTank(){if(!state.story.firstTankRepaired){showDialogue('메인 수조','여과 장치가 멈춰 있다. 브루노에게 먼저 보여주는 게 좋겠다.','🛠');return}openModal('mainTank',tankPanelHtml('메인 수조',state.aquarium.displayed,state.aquarium.mainTankCapacity,'main'))}
function openPlacedTank(uid){const p=state.placed.find(x=>x.uid===uid);if(!p)return;openModal('placedTank',tankPanelHtml(BUILD_DEFS[p.type].name,p.fish||{},BUILD_DEFS[p.type].capacity,'placed',uid))}
function openShop(){ui.shopTab='buy';renderShop()}
function renderShop(){const buy=[{kind:'rod',id:'wide',name:RODS.wide.name,price:RODS.wide.buy,desc:RODS.wide.description,owned:state.equipment.ownedRods.wide},{kind:'item',id:'wideBait',name:ITEMS.wideBait.name,price:ITEMS.wideBait.price,desc:ITEMS.wideBait.description},{kind:'item',id:'curiosityBait',name:ITEMS.curiosityBait.name,price:ITEMS.curiosityBait.price,desc:ITEMS.curiosityBait.description},{kind:'item',id:'iceBomb',name:ITEMS.iceBomb.name,price:ITEMS.iceBomb.price,desc:ITEMS.iceBomb.description}].map(i=>`<div class="panel-card"><b>${i.name}</b>${i.desc}<br><strong>${i.price}G</strong><button data-action="buy" data-kind="${i.kind}" data-id="${i.id}" ${i.owned?'disabled':''}>${i.owned?'보유 중':'구매'}</button></div>`).join(''),sell=Object.entries(state.inventory.fish).filter(([,q])=>q>0).map(([id,q])=>`<div class="panel-card"><b>${SPECIES[id].icon} ${SPECIES[id].name} ×${q}</b>1마리 ${SPECIES[id].price}G<button data-action="sell" data-id="${id}">1마리 판매</button></div>`).join('');openModal('shop',`<h2 class="panel-title">🏪 마르코의 연못 상점 · ${state.money}G</h2><div class="panel-sub">“어서 와. 연못에서 쓸 만한 것들만 골라놨어.”</div><div class="shop-tabs"><button data-action="shopTab" data-id="buy" class="${ui.shopTab==='buy'?'active':''}">물품 구매</button><button data-action="shopTab" data-id="sell" class="${ui.shopTab==='sell'?'active':''}">생물 판매</button></div><div class="panel-grid">${ui.shopTab==='buy'?buy:(sell||'<div class="panel-card">판매할 생물이 없습니다.</div>')}</div>`)}
function openSettlement(s){openModal('settlement',`<h2 class="panel-title">🌅 ${s.day}일 영업 정산</h2><div class="panel-card">방문객 ${s.visitors}명<br>입장료 수익 ${s.revenue}G<br>평균 만족도 ${s.avgSatisfaction}%<hr><b>순수익 ${s.net}G</b></div>`)}
function bindModalActions(){dom.modalContent.querySelectorAll('[data-action]').forEach(btn=>btn.addEventListener('click',()=>handleModalAction(btn.dataset.action,btn.dataset.id,btn.dataset.kind,btn.dataset.uid)))}
function handleModalAction(action,id,kind,uid){if(action==='equipRod'){state.equipment.rod=id;openEquipment();return}if(action==='cycleBait'){const opts=[null,'wideBait','curiosityBait'].filter(x=>!x||itemCount(x)>0);let i=opts.indexOf(state.equipment.bait);state.equipment.bait=opts[(i+1)%opts.length];openEquipment();return}if(action==='ticketMinus'){state.aquarium.ticketPrice=clamp(state.aquarium.ticketPrice-1,0,50);openManager();return}if(action==='ticketPlus'){state.aquarium.ticketPrice=clamp(state.aquarium.ticketPrice+1,0,50);openManager();return}if(action==='sleep6'){closeModal();skipToHour(6);return}if(action==='skipNoon'){closeModal();skipToHour(12);return}if(action==='shopTab'){ui.shopTab=id;renderShop();return}if(action==='buy'){if(kind==='rod'){const r=RODS[id];if(!state.equipment.ownedRods[id]&&state.money>=r.buy){state.money-=r.buy;state.equipment.ownedRods[id]=1;state.equipment.rod=id;showToast(`${r.name} 구매·장착`)}else showToast('골드가 부족합니다.')}else{const it=ITEMS[id];if(state.money<it.price)showToast('골드가 부족합니다.');else if(!bagSpaceFor('item',id))showToast('가방 슬롯이 부족합니다.');else{state.money-=it.price;addItem(id,1);showToast(`${it.name} 구매`)}}renderShop();saveGame();return}if(action==='sell'){const s=SPECIES[id];if(removeFish(id,1)){state.money+=s.price;showToast(`${s.name} 판매 +${s.price}G`)}renderShop();saveGame();return}if(action==='tankAdd'||action==='tankRemove'){let map,cap;if(kind==='main'){map=state.aquarium.displayed;cap=state.aquarium.mainTankCapacity}else{const p=state.placed.find(x=>x.uid===uid);if(!p)return;map=p.fish||(p.fish={});cap=BUILD_DEFS[p.type].capacity}if(action==='tankAdd'){if(displayedTotal(map)<cap&&removeFish(id,1)){map[id]=(map[id]||0)+1;if(state.story.firstTankRepaired&&!state.aquarium.isOpen){state.aquarium.isOpen=true;state.story.localReopening=true;state.story.stage=Math.max(state.story.stage,3);showToast('작은 재개장! 마을 주민들이 찾아오기 시작합니다.')}}}else if((map[id]||0)>0&&bagSpaceFor('fish',id)){map[id]--;if(map[id]<=0)delete map[id];addFish(id,1)}if(kind==='main')openMainTank();else openPlacedTank(uid);saveGame();return}if(action==='place'){startPlacement(id);return}}

// --- HUD ---------------------------------------------------------------------
function questInfo(){if(!state.story.firstTankRepaired)return{title:'다시 물이 흐르도록',text:'브루노에게 말을 걸어 첫 수조를 확인하세요.'};if(state.story.stage<=1)return{title:'첫 번째 생명',text:'연못 물가에서 수심 색을 보고 낚시해 생물을 한 마리 잡아보세요.'};if(state.story.stage===2)return{title:'첫 번째 전시',text:'아쿠아리움 메인 수조에 잡은 생물을 한 마리 전시하세요.'};return{title:'작은 재개장',text:'낚시·전시·판매를 이어가며 아쿠아리움을 키워보세요.'}}
function locationName(){if(state.scene==='shop')return'연못 마을 · 마르코 상점';if(state.scene==='aquarium'){if(rectContains(managerRoom,state.player.x,state.player.y))return'연못 마을 · 아쿠아리움 관리자실';return'연못 마을 · 낡은 아쿠아리움'}const p=state.player;if(p.x<14*TILE&&p.y<23*TILE)return'연못 마을 · 버스정류장';if(pondNorm(p.x,p.y)<1.35)return'연못 마을 · 중앙 연못';if(p.x<36*TILE&&p.y<28*TILE)return'연못 마을 · 아쿠아리움 언덕';if(p.x<60*TILE&&p.y<30*TILE)return'연못 마을 · 상점길';if(p.x<84*TILE&&p.y<30*TILE)return'연못 마을 · 작업장길';return'연못 마을'}
function refreshHud(force=false){const bucket=Math.floor(performance.now()/250);if(!force&&bucket===ui.lastHud)return;ui.lastHud=bucket;dom.dateText.textContent=`${state.day}일`;dom.timeText.textContent=fmtTime(state.minute);dom.weatherText.textContent=`${currentWeather().icon} ${currentWeather().name}`;dom.locationText.textContent=locationName();dom.moneyText.textContent=`${state.money}G`;const q=questInfo();dom.questTitle.textContent=q.title;dom.questText.textContent=q.text;const stacks=[];for(const[id,n]of Object.entries(state.inventory.fish))if(n>0)stacks.push({icon:SPECIES[id].icon,name:SPECIES[id].name,q:n});for(const[id,n]of Object.entries(state.inventory.items))if(n>0)stacks.push({icon:ITEMS[id].icon,name:ITEMS[id].name,q:n});dom.bagUsage.textContent=`${stacks.length}/${state.inventory.capacity}`;dom.bagGrid.innerHTML=Array.from({length:state.inventory.capacity},(_,i)=>{const s=stacks[i];return s?`<div class="bag-slot" title="${s.name}"><span class="icon">${s.icon}</span><span class="name">${s.name}</span><span class="qty">×${s.q}</span></div>`:'<div class="bag-slot empty"></div>'}).join('');dom.buildButton.classList.toggle('hidden',state.scene!=='aquarium');dom.iceBombCount.textContent=`×${itemCount('iceBomb')}`;updateInteractionHint()}
function updateInteractionHint(){if(ui.modal||fishing||buildState.active||cast.mode!=='idle'){dom.interactionHint.classList.add('hidden');return}let text='';if(state.scene==='village'){let best=null,bd=76;for(const n of npcs){const d=dist(state.player.x,state.player.y,n.x,n.y);if(d<bd){best=n;bd=d}}if(best)text=`좌클릭 · ${best.name}와 대화`;else{for(const b of buildings){const r=tileRect(b),qx=clamp(state.player.x,r.x,r.x+r.w),qy=clamp(state.player.y,r.y,r.y+r.h);if(dist(state.player.x,state.player.y,qx,qy)<78){text=`좌클릭 · ${b.label}`;break}}if(!text&&canStartCast())text='좌클릭 홀드 · 캐스팅'}}else if(state.scene==='shop'){if(rectContains(shopServiceZone,state.player.x,state.player.y,22))text='좌클릭 · 계산대에서 상점 이용';else if(rectContains(shopExit,state.player.x,state.player.y,90))text='좌클릭 · 상점 밖으로'}else{if(rectContains(managerRoom,state.player.x,state.player.y,30))text='관리자실 · 장부를 좌클릭';else if(dist(state.player.x,state.player.y,aquariumTank.x,aquariumTank.y)<190)text='수조 가까이에서 좌클릭'}if(text){dom.interactionHint.innerHTML=`<b>●</b> ${text}`;dom.interactionHint.classList.remove('hidden')}else dom.interactionHint.classList.add('hidden')}

// --- Rendering ---------------------------------------------------------------
const w2s=(x,y)=>({x:x-camera.x,y:y-camera.y});
const visible=(rc,p=120)=>rc.x+rc.w>=camera.x-p&&rc.x<=camera.x+VIEW.w+p&&rc.y+rc.h>=camera.y-p&&rc.y<=camera.y+VIEW.h+p;
function pixelText(text,x,y,size=12,color='#fff7d2',align='left'){ctx.save();ctx.font=`${size}px Galmuri11, monospace`;ctx.textAlign=align;ctx.textBaseline='alphabetic';ctx.fillStyle='#15221fcc';ctx.fillText(text,x+2,y+2);ctx.fillStyle=color;ctx.fillText(text,x,y);ctx.restore()}
function lighting(){const h=gameHour();let dark=0,tint=null,shadowLen=7;if(h>=7&&h<16){shadowLen=8}else if(h>=16&&h<20){const t=(h-16)/4;dark=.03+t*.18;tint=`rgba(224,126,70,${.04+t*.12})`;shadowLen=8+18*t}else if(h>=4&&h<7){const t=(h-4)/3;dark=.32*(1-t);tint=`rgba(88,108,151,${.13*(1-t)})`;shadowLen=25-17*t}else{dark=.47;tint='rgba(32,52,96,.22)';shadowLen=5}if(state.weather==='cloudy')dark+=.06;if(state.weather==='rain')dark+=.11;return{dark:clamp(dark,0,.62),tint,shadowLen}}
const shadowAlpha=()=>(state.weather==='sunny'?.24:state.weather==='cloudy'?.12:.07)*(1-lighting().dark*.55);

function traceWorldPoly(points){ctx.beginPath();points.forEach(([x,y],i)=>{const sx=x*TILE-camera.x,sy=y*TILE-camera.y;i?ctx.lineTo(sx,sy):ctx.moveTo(sx,sy)});ctx.closePath()}
function drawTerraceEdge(points){
  ctx.save();const top=points.map(([x,y])=>[x*TILE-camera.x,y*TILE-camera.y]);
  ctx.beginPath();top.forEach(([x,y],i)=>i?ctx.lineTo(x,y):ctx.moveTo(x,y));for(let i=top.length-1;i>=0;i--)ctx.lineTo(top[i][0],top[i][1]+22);ctx.closePath();ctx.fillStyle='#506b4d';ctx.fill();
  ctx.strokeStyle='#334a37';ctx.lineWidth=3;ctx.beginPath();top.forEach(([x,y],i)=>i?ctx.lineTo(x,y+20):ctx.moveTo(x,y+20));ctx.stroke();
  for(let i=0;i<top.length-1;i++){const[x1,y1]=top[i],[x2,y2]=top[i+1],len=Math.hypot(x2-x1,y2-y1);if(len<8)continue;const count=Math.floor(len/42);for(let j=0;j<count;j++){const t=(j+.35)/(count+.2),x=lerp(x1,x2,t),y=lerp(y1,y2,t)+8+(j%2)*5;ctx.fillStyle=j%2?'#61785a':'#415d45';ctx.fillRect(Math.round(x),Math.round(y),13+(j%3)*4,5)}}ctx.restore();
}
function drawStair(tx,ty,w=3,steps=6){const x=tx*TILE-camera.x,y=ty*TILE-camera.y,ww=w*TILE;ctx.fillStyle='#b99a69';ctx.fillRect(x,y,ww,steps*7);for(let i=0;i<steps;i++){ctx.fillStyle=i%2?'#a9895c':'#c3a571';ctx.fillRect(x,y+i*7,ww,5);ctx.fillStyle='rgba(71,55,37,.25)';ctx.fillRect(x,y+i*7+5,ww,2)}}
function drawVillageGround(){
  ctx.fillStyle='#6f9b63';ctx.fillRect(0,0,VIEW.w,VIEW.h);
  ctx.save();ctx.translate(-camera.x,-camera.y);ctx.fillStyle='#526f59';for(let x=-220;x<CONFIG.villageCols*TILE+420;x+=430){ctx.beginPath();ctx.moveTo(x,260);ctx.lineTo(x+125,78+(Math.abs(x/430)%3)*32);ctx.lineTo(x+245,145);ctx.lineTo(x+430,260);ctx.closePath();ctx.fill()}ctx.fillStyle='#63865e';ctx.fillRect(0,205,CONFIG.villageCols*TILE,95);ctx.restore();
  const upper=[[0,8],[13,8],[13,10],[29,10],[29,8],[44,8],[44,10],[62,10],[62,7],[82,7],[82,9],[99,9],[99,12],[112,12],[112,0],[0,0]];
  const middle=[[0,31],[12,31],[12,29],[27,29],[27,32],[43,32],[43,28],[59,28],[59,31],[78,31],[78,27],[94,27],[94,30],[112,30],[112,12],[99,12],[99,9],[82,9],[82,7],[62,7],[62,10],[44,10],[44,8],[29,8],[29,10],[13,10],[13,8],[0,8]];
  const lower=[[0,50],[16,50],[16,47],[32,47],[32,51],[49,51],[49,46],[63,46],[63,49],[79,49],[79,44],[95,44],[95,48],[112,48],[112,30],[94,30],[94,27],[78,27],[78,31],[59,31],[59,28],[43,28],[43,32],[27,32],[27,29],[12,29],[12,31],[0,31]];
  traceWorldPoly(upper);ctx.fillStyle='#719d65';ctx.fill();traceWorldPoly(middle);ctx.fillStyle='#7ba86a';ctx.fill();traceWorldPoly(lower);ctx.fillStyle='#84b372';ctx.fill();
  ctx.save();ctx.translate(-camera.x,-camera.y);ctx.fillStyle='#8aba76';ctx.fillRect(0,48*TILE,CONFIG.villageCols*TILE,(CONFIG.villageRows-48)*TILE);ctx.restore();
  drawTerraceEdge([[0,31],[12,31],[12,29],[27,29],[27,32],[43,32],[43,28],[59,28],[59,31],[78,31],[78,27],[94,27],[94,30],[112,30]]);drawTerraceEdge([[0,50],[16,50],[16,47],[32,47],[32,51],[49,51],[49,46],[63,46],[63,49],[79,49],[79,44],[95,44],[95,48],[112,48]]);
  drawStair(46,29,3,7);drawStair(79,28,3,7);drawStair(30,48,3,7);drawStair(61,47,3,7);drawStair(93,45,3,7);
  ctx.save();ctx.translate(-camera.x,-camera.y);
  for(const [key] of pathTiles){const[tx,ty]=key.split(',').map(Number),x=tx*TILE,y=ty*TILE;if(!visible(rect(x,y,TILE,TILE),32))continue;const n=(tx*17+ty*29)%5;ctx.fillStyle=['#c6a66f','#ceb27b','#bea06a','#d0b47d','#c8aa72'][n];ctx.fillRect(x,y,TILE,TILE);ctx.strokeStyle='rgba(103,76,44,.19)';ctx.strokeRect(x+.5,y+.5,TILE-1,TILE-1);ctx.fillStyle='rgba(244,220,170,.28)';ctx.fillRect(x+3+(n*6)%19,y+5+(n*9)%18,6,3)}
  for(const g of grassTufts){if(pondNorm(g.x,g.y)<1.12)continue;if(pathTiles.has(`${Math.floor(g.x/TILE)},${Math.floor(g.y/TILE)}`))continue;ctx.fillStyle=g.t>.65?'#5f8c55':'#6e9d5e';ctx.fillRect(Math.round(g.x),Math.round(g.y),2,5);ctx.fillRect(Math.round(g.x+4),Math.round(g.y+2),2,4)}
  for(const f of flowerPatches){if(pondNorm(f.x,f.y)<1.1)continue;if(pathTiles.has(`${Math.floor(f.x/TILE)},${Math.floor(f.y/TILE)}`))continue;ctx.fillStyle='#4e7d4e';ctx.fillRect(Math.round(f.x),Math.round(f.y),2,7);ctx.fillStyle=f.c;ctx.fillRect(Math.round(f.x-2),Math.round(f.y-2),6,4)}ctx.restore();drawPond();drawBusStop();
}
function pondScreenPath(scale=1){ctx.beginPath();const steps=112;for(let i=0;i<=steps;i++){const a=i/steps*Math.PI*2,edge=pondBoundaryScale(a)*scale,x=pond.cx+Math.cos(a)*pond.rx*edge-camera.x,y=pond.cy+Math.sin(a)*pond.ry*edge-camera.y;i?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.closePath()}
function drawPond(){
  pondScreenPath(1.025);ctx.fillStyle='#4c6d50';ctx.fill();pondScreenPath(1);ctx.fillStyle='#87cfc2';ctx.fill();pondScreenPath(.72);ctx.fillStyle='#559fb2';ctx.fill();pondScreenPath(.42);ctx.fillStyle='#316782';ctx.fill();
  pondScreenPath(1);ctx.strokeStyle='rgba(226,244,213,.62)';ctx.lineWidth=3;ctx.stroke();pondScreenPath(.72);ctx.strokeStyle='rgba(205,240,234,.27)';ctx.lineWidth=2;ctx.stroke();pondScreenPath(.42);ctx.strokeStyle='rgba(20,67,91,.28)';ctx.stroke();
  for(const st of shoreStones){const q=w2s(st.x,st.y);ctx.fillStyle='#43534d';ctx.beginPath();ctx.ellipse(q.x,q.y,st.s,st.s*.56,-.2,0,Math.PI*2);ctx.fill();ctx.fillStyle='#67756d';ctx.fillRect(q.x-st.s*.45,q.y-st.s*.3,st.s*.8,2)}
  for(const r of shoreReeds){const q=w2s(r.x,r.y);ctx.strokeStyle='#537d4d';ctx.lineWidth=2;for(let i=-1;i<=1;i++){ctx.beginPath();ctx.moveTo(q.x+i*3,q.y+4);ctx.lineTo(q.x+i*4-2,q.y-r.h+i*2);ctx.stroke()}ctx.fillStyle='#6e8b50';ctx.fillRect(q.x+2,q.y-r.h-4,3,7)}
  for(const l of lilyPads){const q=w2s(l.x,l.y);ctx.fillStyle='#4e9569';ctx.beginPath();ctx.ellipse(q.x,q.y,l.s,l.s*.55,-.15,0,Math.PI*2);ctx.fill();ctx.strokeStyle='rgba(34,91,70,.55)';ctx.beginPath();ctx.moveTo(q.x,q.y);ctx.lineTo(q.x+l.s*.8,q.y-1);ctx.stroke();if(l.flower){ctx.fillStyle='#f0bfd1';ctx.fillRect(q.x-2,q.y-3,5,5)}}
  ctx.save();ctx.globalAlpha=.34;ctx.strokeStyle='#e7fbef';ctx.lineWidth=2;for(const sp of waterSparkles){const a=sp.a+state.absoluteMinutes*.0018+sp.p,edge=pondBoundaryScale(a),x=pond.cx+Math.cos(a)*pond.rx*edge*sp.r-camera.x,y=pond.cy+Math.sin(a)*pond.ry*edge*sp.r-camera.y;ctx.beginPath();ctx.moveTo(x-7,y);ctx.lineTo(x+7,y);ctx.stroke()}for(const rp of pondRipples){const q=w2s(rp.x,rp.y),pulse=4+((performance.now()/240+rp.p*8)%14);ctx.beginPath();ctx.ellipse(q.x,q.y,pulse,pulse*.45,0,0,Math.PI*2);ctx.stroke()}ctx.restore();
}
function drawBusStop(){const s=w2s(busShelter.x,busShelter.y),L=lighting();ctx.fillStyle=`rgba(40,42,31,${shadowAlpha()})`;ctx.fillRect(s.x+L.shadowLen,s.y+48,busShelter.w,36);ctx.fillStyle='#6b4a30';ctx.fillRect(s.x,s.y+31,busShelter.w,8);ctx.fillRect(s.x+8,s.y+39,7,56);ctx.fillRect(s.x+busShelter.w-15,s.y+39,7,56);ctx.fillStyle='#d6bd82';ctx.fillRect(s.x+30,s.y+66,80,9);ctx.fillStyle='#294d63';ctx.fillRect(s.x+busShelter.w+12,s.y+2,7,94);ctx.fillStyle='#f3e3a9';ctx.fillRect(s.x+busShelter.w-3,s.y+2,37,28);pixelText('BUS',s.x+busShelter.w+15,s.y+21,9,'#294d63','center');pixelText('버스정류장',s.x+busShelter.w*.5,s.y+116,9,'#fff0bd','center')}
function drawBuilding(b){
  const r=tileRect(b);if(!visible(rect(r.x-35,r.y-55,r.w+80,r.h+95)))return;const s=w2s(r.x,r.y),L=lighting(),depth=24;
  ctx.fillStyle=`rgba(34,38,29,${shadowAlpha()})`;ctx.beginPath();ctx.moveTo(s.x+18+L.shadowLen,s.y+70+L.shadowLen*.4);ctx.lineTo(s.x+r.w+30+L.shadowLen,s.y+70+L.shadowLen*.4);ctx.lineTo(s.x+r.w+12+L.shadowLen,s.y+r.h+24);ctx.lineTo(s.x+2+L.shadowLen,s.y+r.h+24);ctx.closePath();ctx.fill();
  ctx.fillStyle='#6d614e';ctx.fillRect(s.x+2,s.y+r.h-20,r.w-4,20);for(let x=8;x<r.w-10;x+=30){ctx.fillStyle=(x/30)%2?'#756854':'#625847';ctx.fillRect(s.x+x,s.y+r.h-17,22,7)}
  ctx.fillStyle='#8c704d';ctx.beginPath();ctx.moveTo(s.x+r.w-10,s.y+72);ctx.lineTo(s.x+r.w+depth,s.y+58);ctx.lineTo(s.x+r.w+depth,s.y+r.h-17);ctx.lineTo(s.x+r.w-10,s.y+r.h);ctx.closePath();ctx.fill();
  ctx.fillStyle=b.wall;ctx.fillRect(s.x,s.y+66,r.w-8,r.h-80);ctx.fillStyle='#65462e';ctx.fillRect(s.x,s.y+64,9,r.h-64);ctx.fillRect(s.x+r.w-17,s.y+64,9,r.h-64);for(let x=52;x<r.w-42;x+=72)ctx.fillRect(s.x+x,s.y+68,6,r.h-87);
  ctx.fillStyle='#3f342a';ctx.beginPath();ctx.moveTo(s.x-24,s.y+70);ctx.lineTo(s.x+r.w*.5,s.y-18);ctx.lineTo(s.x+r.w+30,s.y+70);ctx.lineTo(s.x+r.w+18,s.y+82);ctx.lineTo(s.x-18,s.y+82);ctx.closePath();ctx.fill();
  ctx.fillStyle=b.roof;ctx.beginPath();ctx.moveTo(s.x-20,s.y+66);ctx.lineTo(s.x+r.w*.5,s.y-14);ctx.lineTo(s.x+r.w*.5,s.y+68);ctx.lineTo(s.x-20,s.y+75);ctx.closePath();ctx.fill();
  ctx.fillStyle='#5f4336';ctx.beginPath();ctx.moveTo(s.x+r.w*.5,s.y-14);ctx.lineTo(s.x+r.w+26,s.y+66);ctx.lineTo(s.x+r.w+18,s.y+76);ctx.lineTo(s.x+r.w*.5,s.y+68);ctx.closePath();ctx.fill();
  ctx.strokeStyle='rgba(255,237,190,.18)';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(s.x+r.w*.5,s.y-11);ctx.lineTo(s.x-16,s.y+67);ctx.stroke();
  if(b.kind!=='shop'){ctx.fillStyle='#665148';ctx.fillRect(s.x+r.w*.72,s.y+2,23,49);ctx.fillStyle='#44362f';ctx.fillRect(s.x+r.w*.72-3,s.y-2,29,8);ctx.fillStyle='rgba(223,223,210,.12)';ctx.fillRect(s.x+r.w*.72+5,s.y+8,5,33)}
  for(let x=38;x<r.w-55;x+=82){ctx.fillStyle='#543d2e';ctx.fillRect(s.x+x-4,s.y+94,39,36);ctx.fillStyle=isNight()?'#f2cf79':'#afd8d5';ctx.fillRect(s.x+x,s.y+98,31,27);ctx.fillStyle='rgba(255,255,255,.18)';ctx.fillRect(s.x+x+4,s.y+102,5,18);ctx.fillStyle='#7f4e3e';ctx.fillRect(s.x+x-2,s.y+129,35,7);ctx.fillStyle='#659653';ctx.fillRect(s.x+x+6,s.y+122,5,9);ctx.fillRect(s.x+x+21,s.y+121,5,10)}
  const doorX=s.x+r.w*.5-19;ctx.fillStyle='#523723';ctx.fillRect(doorX,s.y+r.h-82,38,62);ctx.fillStyle='#775036';ctx.fillRect(doorX+4,s.y+r.h-77,30,57);ctx.fillStyle='#e3bb61';ctx.fillRect(doorX+28,s.y+r.h-51,4,4);ctx.fillStyle='#7a593b';ctx.fillRect(doorX-13,s.y+r.h-21,64,10);ctx.fillStyle='#9e7d55';ctx.fillRect(doorX-20,s.y+r.h-11,78,9);
  if(b.sign){ctx.fillStyle='#523b2a';ctx.fillRect(s.x+r.w*.5-65,s.y+66,130,27);pixelText(b.sign,s.x+r.w*.5,s.y+85,10,'#ffe6a5','center')}
  if(b.kind==='shop'){ctx.fillStyle='#7d5132';ctx.fillRect(s.x+15,s.y+r.h-42,62,34);ctx.fillStyle='#caa661';ctx.fillRect(s.x+12,s.y+r.h-48,68,7);for(let i=0;i<3;i++){ctx.fillStyle=['#d8bb61','#86aa63','#d98b78'][i];ctx.fillRect(s.x+23+i*16,s.y+r.h-34,11,12)}}
  if(b.kind==='workshop'){ctx.fillStyle='#69482f';ctx.fillRect(s.x+r.w-78,s.y+r.h-48,60,37);ctx.fillStyle='#a5aca8';ctx.fillRect(s.x+r.w-57,s.y+r.h-72,8,29);ctx.fillRect(s.x+r.w-65,s.y+r.h-70,25,7)}
  if(b.kind==='aquarium'){ctx.fillStyle='#718278';ctx.fillRect(s.x+15,s.y+r.h-51,27,49);ctx.fillStyle='#9d8d75';ctx.fillRect(s.x+r.w-40,s.y+r.h-62,22,61);ctx.fillStyle='#b8c8bd';ctx.fillRect(s.x+r.w*.5-66,s.y+37,132,22);pixelText('낚시 아쿠아리움',s.x+r.w*.5,s.y+53,9,'#405149','center')}
}
function drawTree(t){const s=w2s(t.x,t.y);if(s.x<-90||s.y<-120||s.x>VIEW.w+90||s.y>VIEW.h+100)return;const L=lighting();ctx.fillStyle=`rgba(42,47,33,${shadowAlpha()})`;ctx.beginPath();ctx.ellipse(s.x+L.shadowLen,s.y+30,31*t.s,10*t.s,0,0,Math.PI*2);ctx.fill();ctx.fillStyle='#68472d';ctx.fillRect(s.x-6*t.s,s.y,12*t.s,40*t.s);const c1=t.blossom?'#dca3b8':'#4f8559',c2=t.blossom?'#efbdca':'#66a064';for(const[dx,dy,r]of[[-18,-12,19],[12,-20,22],[0,-39,21],[24,-4,16],[-27,-31,16]]){ctx.fillStyle=((dx+dy)%3)?c1:c2;ctx.beginPath();ctx.arc(s.x+dx*t.s,s.y+dy*t.s,r*t.s,0,Math.PI*2);ctx.fill()}}
function drawRock(rc){const s=w2s(rc.x,rc.y);ctx.fillStyle=`rgba(32,38,30,${shadowAlpha()})`;ctx.beginPath();ctx.ellipse(s.x+rc.w*.55,s.y+rc.h*.9,rc.w*.5,rc.h*.25,0,0,Math.PI*2);ctx.fill();ctx.fillStyle='#4e5a53';ctx.beginPath();ctx.moveTo(s.x+3,s.y+rc.h);ctx.lineTo(s.x+rc.w*.2,s.y+rc.h*.28);ctx.lineTo(s.x+rc.w*.62,s.y+2);ctx.lineTo(s.x+rc.w,s.y+rc.h*.45);ctx.lineTo(s.x+rc.w*.9,s.y+rc.h);ctx.closePath();ctx.fill()}

function drawPerson(x,y,style,dir,moving,name,isPlayer=false){
  const s=w2s(x,y);if(s.x<-55||s.y<-75||s.x>VIEW.w+55||s.y>VIEW.h+75)return;
  const frame=moving?Math.floor(performance.now()/140)%2:0,bob=moving?(frame?1:-1):0,step=moving?(frame?3:-3):0,skin=style.skin||'#e2b18c',hair=style.hair||'#49352f',body=style.body||'#416a91',accent=style.accent||'#fff';
  ctx.save();ctx.translate(Math.round(s.x),Math.round(s.y+bob));ctx.fillStyle='rgba(26,31,26,.34)';ctx.beginPath();ctx.ellipse(0,20,15,6,0,0,Math.PI*2);ctx.fill();
  if(isPlayer){ctx.fillStyle='#4c3729';if(dir==='up')ctx.fillRect(-11,-7,22,20);else if(dir==='left')ctx.fillRect(7,-6,8,18);else if(dir==='right')ctx.fillRect(-15,-6,8,18);else{ctx.fillRect(-12,-3,5,14);ctx.fillRect(7,-3,5,14)}}
  ctx.fillStyle='#342820';ctx.fillRect(-10,-24,20,21);ctx.fillStyle=skin;ctx.fillRect(-8,-21,16,15);ctx.fillStyle=hair;ctx.fillRect(-10,-26,20,8);ctx.fillRect(-10,-21,4,9);ctx.fillRect(6,-21,4,9);ctx.fillRect(-6,-28,12,3);
  ctx.fillStyle='#33271f';ctx.fillRect(-11,-5,22,23);ctx.fillStyle=body;ctx.fillRect(-9,-3,18,19);
  if(isPlayer){ctx.fillStyle='#f1ead4';ctx.fillRect(-9,-3,18,7);ctx.fillRect(-12,0,4,11);ctx.fillRect(8,0,4,11);ctx.fillStyle='#416a91';ctx.fillRect(-6,3,12,14);ctx.fillRect(-8,1,3,15);ctx.fillRect(5,1,3,15);ctx.fillStyle='#d8c06d';ctx.fillRect(-5,5,2,2);ctx.fillRect(3,5,2,2);ctx.fillStyle='#315678';ctx.fillRect(-5,9,10,2)}else{ctx.fillStyle=accent;ctx.fillRect(-8,0,16,5);ctx.fillStyle=skin;ctx.fillRect(-12,1,3,10);ctx.fillRect(9,1,3,10)}
  ctx.fillStyle='#3a2b23';ctx.fillRect(-8+step,16,6,10);ctx.fillRect(2-step,16,6,10);ctx.fillStyle=isPlayer?'#5d3f2a':'#4a3528';ctx.fillRect(-9+step,24,7,5);ctx.fillRect(2-step,24,7,5);
  if(dir!=='up'){const eyeShift=dir==='left'?-2:dir==='right'?2:0;ctx.fillStyle='#f7f1df';if(isPlayer){ctx.fillRect(-5+eyeShift,-16,4,3);ctx.fillRect(2+eyeShift,-16,4,3)}ctx.fillStyle='#26231f';ctx.fillRect(-4+eyeShift,-15,2,2);ctx.fillRect(3+eyeShift,-15,2,2);ctx.fillStyle='#b86f61';ctx.fillRect(-1+eyeShift,-10,3,1)}
  ctx.restore();if(name)pixelText(name,s.x,s.y-35,9,'#fff0bd','center')
}
function drawEntities(){const list=[];if(state.scene==='village'){for(const n of npcs)list.push({y:n.y,fn:()=>drawPerson(n.x,n.y,n.style,n.dir,n.moving,n.id.startsWith('resident')?null:n.name,false)})}else if(state.scene==='shop'){list.push({y:shopMarco.y,fn:()=>drawPerson(shopMarco.x,shopMarco.y,shopMarco.style,shopMarco.dir,false,'마르코',false)})}else{for(const v of visualVisitors)list.push({y:v.y,fn:()=>drawPerson(v.x,v.y,{hair:'#514038',body:v.shirt,accent:'#e4d39e',skin:'#dca67c'},v.dir,true,null,false)})}list.push({y:state.player.y,fn:()=>drawPerson(state.player.x,state.player.y,{hair:'#4b342c',body:'#416a91',accent:'#f1ead4',skin:'#efc29d'},state.player.dir,input.keys.size>0&&!ui.modal&&!buildState.active,null,true)});list.sort((a,b)=>a.y-b.y);for(const e of list)e.fn()}
function drawCastStuff(){if(state.scene!=='village')return;if(cast.mode==='charging'&&cast.landing){const a=w2s(state.player.x,state.player.y),b=w2s(cast.landing.x,cast.landing.y);ctx.save();ctx.setLineDash([7,7]);ctx.strokeStyle='#fff0a8d9';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(a.x,a.y-10);ctx.lineTo(b.x,b.y);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle=cast.depth?DEPTH[cast.depth].color:'#cf6b64';ctx.beginPath();ctx.arc(b.x,b.y,9,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#fff5cf';ctx.stroke();ctx.restore()}if(['waiting','bite'].includes(cast.mode)&&cast.landing){const b=w2s(cast.landing.x,cast.landing.y);ctx.save();ctx.translate(b.x,b.y+Math.sin(cast.pulse)*2);ctx.fillStyle='#f6f0de';ctx.fillRect(-3,-8,6,8);ctx.fillStyle='#d95b4e';ctx.fillRect(-3,-12,6,5);ctx.fillStyle='#283d47';ctx.fillRect(-1,-17,2,5);ctx.restore()}}
function drawVillage(){
  drawVillageGround();drawCastStuff();
  const list=[];
  for(const b of buildings){const r=tileRect(b);list.push({y:r.y+r.h,fn:()=>drawBuilding(b)})}
  for(const r of rocks)list.push({y:r.y+r.h,fn:()=>drawRock(r)});
  for(const t of trees)list.push({y:t.y+40,fn:()=>drawTree(t)});
  for(const n of npcs)list.push({y:n.y,fn:()=>drawPerson(n.x,n.y,n.style,n.dir,n.moving,n.id.startsWith('resident')?null:n.name,false)});
  list.push({y:state.player.y,fn:()=>drawPerson(state.player.x,state.player.y,{hair:'#4b342c',body:'#416a91',accent:'#f1ead4',skin:'#efc29d'},state.player.dir,input.keys.size>0&&!ui.modal&&!buildState.active,null,true)});
  list.sort((a,b)=>a.y-b.y);for(const e of list)e.fn()
}

function drawAquarium(){ctx.fillStyle='#775d43';ctx.fillRect(0,0,VIEW.w,VIEW.h);ctx.save();ctx.translate(-camera.x,-camera.y);const W=CONFIG.aquariumCols*TILE,H=CONFIG.aquariumRows*TILE;ctx.fillStyle='#d3b983';ctx.fillRect(2*TILE,2*TILE,W-4*TILE,H-4*TILE);for(let x=2*TILE;x<W-2*TILE;x+=TILE){ctx.strokeStyle='rgba(112,82,49,.16)';ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(x,2*TILE);ctx.lineTo(x,H-2*TILE);ctx.stroke()}for(let y=2*TILE;y<H-2*TILE;y+=TILE){ctx.strokeStyle='rgba(112,82,49,.1)';ctx.beginPath();ctx.moveTo(2*TILE,y);ctx.lineTo(W-2*TILE,y);ctx.stroke()}ctx.restore();drawInteriorTank();drawManagerRoom();drawPlaced();drawEntities();const e=w2s(aquariumExit.x,aquariumExit.y);ctx.fillStyle='#6a4830';ctx.fillRect(e.x,e.y,aquariumExit.w,aquariumExit.h);pixelText('마을로',e.x+aquariumExit.w/2,e.y+aquariumExit.h/2+4,9,'#ffe9ad','center')}
function drawInteriorTank(){const s=w2s(aquariumTank.x,aquariumTank.y),L=lighting();ctx.fillStyle=`rgba(40,40,30,${shadowAlpha()})`;ctx.fillRect(s.x+L.shadowLen,s.y+L.shadowLen*.5,aquariumTank.w,aquariumTank.h);ctx.fillStyle=state.story.firstTankRepaired?'rgba(39,109,126,.84)':'rgba(66,84,84,.72)';ctx.fillRect(s.x,s.y,aquariumTank.w,aquariumTank.h);ctx.strokeStyle='#49382a';ctx.lineWidth=10;ctx.strokeRect(s.x,s.y,aquariumTank.w,aquariumTank.h);if(state.story.firstTankRepaired){let i=0;for(const[id,q]of Object.entries(state.aquarium.displayed))for(let k=0;k<q&&i<20;k++,i++)drawTinyFish(s.x+45+(i*67%(aquariumTank.w-90)),s.y+42+((i*43)%(aquariumTank.h-75)),SPECIES[id].rarity==='고급'?'#e6c268':'#d4ddbb',i%2?1:-1)}pixelText('메인 수조',s.x+aquariumTank.w*.5,s.y-10,10,'#fff0bd','center')}
function drawManagerRoom(){
  const r=w2s(managerRoom.x,managerRoom.y);ctx.fillStyle='#9d744d';ctx.fillRect(r.x,r.y,managerRoom.w,managerRoom.h);
  for(let y=0;y<managerRoom.h;y+=TILE){ctx.fillStyle=(y/TILE)%2?'rgba(128,88,53,.16)':'rgba(255,228,174,.08)';ctx.fillRect(r.x,r.y+y,managerRoom.w,TILE)}
  for(const w of managerWalls){const s=w2s(w.x,w.y);ctx.fillStyle='#493525';ctx.fillRect(s.x,s.y,w.w,w.h);ctx.fillStyle='#76533a';ctx.fillRect(s.x,s.y,w.w,Math.min(5,w.h))}
  const door=w2s(managerDoor.x,managerDoor.y);ctx.fillStyle='#3d2b20';ctx.fillRect(door.x-5,door.y-8,managerDoor.w+10,12);ctx.fillStyle='#815a3c';ctx.fillRect(door.x+7,door.y-44,managerDoor.w*.72,40);ctx.fillStyle='#d9b764';ctx.fillRect(door.x+managerDoor.w*.72,door.y-25,4,4);pixelText('관계자 외 출입금지',door.x+managerDoor.w/2,door.y-54,9,'#ffe3a1','center');
  const d=w2s(managerDesk.x,managerDesk.y);ctx.fillStyle='#65432b';ctx.fillRect(d.x,d.y,managerDesk.w,managerDesk.h);ctx.fillStyle='#d9bc79';ctx.fillRect(d.x+14,d.y+10,44,28);ctx.fillStyle='#b99a61';ctx.fillRect(d.x+68,d.y+10,14,14)
}
function drawPlaced(){for(const p of state.placed){const rc=placedRect(p),s=w2s(rc.x,rc.y);if(p.type==='rug'){ctx.fillStyle='#b96f6c';ctx.fillRect(s.x,s.y,rc.w,rc.h);ctx.fillStyle='#e6b58b';for(let x=5;x<rc.w;x+=18)ctx.fillRect(s.x+x,s.y+5,5,rc.h-10)}else if(p.type==='plant'){ctx.fillStyle='#855739';ctx.fillRect(s.x+7,s.y+18,18,14);ctx.fillStyle='#4f8a58';ctx.fillRect(s.x+13,s.y+2,6,20);ctx.fillRect(s.x+5,s.y+6,10,8);ctx.fillRect(s.x+17,s.y+5,10,9)}else if(p.type==='smallTank'){ctx.fillStyle='rgba(42,121,137,.82)';ctx.fillRect(s.x,s.y,rc.w,rc.h);ctx.strokeStyle='#49382a';ctx.lineWidth=5;ctx.strokeRect(s.x,s.y,rc.w,rc.h);let i=0;for(const[id,q]of Object.entries(p.fish||{}))for(let k=0;k<q;k++,i++)drawTinyFish(s.x+18+(i*21%(Math.max(31,rc.w-30))),s.y+12+((i*13)%(Math.max(21,rc.h-20))),SPECIES[id].rarity==='고급'?'#e5c266':'#d8e2c2',i%2?1:-1)}}}
function drawTinyFish(x,y,color,dir){ctx.fillStyle=color;ctx.fillRect(x-7,y-3,14,6);ctx.beginPath();ctx.moveTo(x-7*dir,y);ctx.lineTo(x-12*dir,y-6);ctx.lineTo(x-12*dir,y+6);ctx.closePath();ctx.fill();ctx.fillStyle='#263331';ctx.fillRect(x+4*dir,y-1,2,2)}

function drawShop(){ctx.fillStyle='#6b5139';ctx.fillRect(0,0,VIEW.w,VIEW.h);ctx.save();ctx.translate(-camera.x,-camera.y);const W=CONFIG.shopCols*TILE,H=CONFIG.shopRows*TILE;ctx.fillStyle='#dfc48e';ctx.fillRect(2*TILE,2*TILE,W-4*TILE,H-4*TILE);for(let x=2*TILE;x<W-2*TILE;x+=TILE){ctx.strokeStyle='rgba(110,78,45,.13)';ctx.strokeRect(x,2*TILE,TILE,H-4*TILE)}ctx.restore();const c=w2s(shopCounter.x,shopCounter.y);ctx.fillStyle='#6c472d';ctx.fillRect(c.x,c.y,shopCounter.w,shopCounter.h);ctx.fillStyle='#a77746';ctx.fillRect(c.x,c.y,shopCounter.w,12);pixelText('마르코의 계산대',c.x+shopCounter.w/2,c.y-10,10,'#ffe4a2','center');for(const sh of shopShelves){const s=w2s(sh.x,sh.y);ctx.fillStyle='#6f4a31';ctx.fillRect(s.x,s.y,sh.w,sh.h);ctx.fillStyle='#b3814d';for(let y=10;y<sh.h-8;y+=28){ctx.fillRect(s.x+5,s.y+y,sh.w-10,5);for(let x=10;x<sh.w-12;x+=21){ctx.fillStyle=((x+y)/7)%2?'#d2b35e':'#6f9b5c';ctx.fillRect(s.x+x,s.y+y-12,9,10)}}}const e=w2s(shopExit.x,shopExit.y);ctx.fillStyle='#65432b';ctx.fillRect(e.x,e.y,shopExit.w,shopExit.h);pixelText('마을로 나가기',e.x+shopExit.w/2,e.y+shopExit.h/2+4,9,'#ffe9ad','center');ctx.fillStyle='#b76558';ctx.fillRect(w2s(14*TILE,11*TILE).x,w2s(14*TILE,11*TILE).y,6*TILE,2*TILE);pixelText('미끼 · 낚시용품',w2s(17*TILE,12*TILE).x,w2s(17*TILE,12*TILE).y,10,'#fff0bd','center');drawEntities()}

function drawBuildGhost(){if(!buildState.active)return;const d=BUILD_DEFS[buildState.type],swap=buildState.rot%2===1,rc=rect(buildState.tx*TILE,buildState.ty*TILE,(swap?d.th:d.tw)*TILE,(swap?d.tw:d.th)*TILE),s=w2s(rc.x,rc.y);ctx.save();ctx.globalAlpha=.55;ctx.fillStyle=buildState.valid?'#9fe0b3':'#e28d82';ctx.fillRect(s.x,s.y,rc.w,rc.h);ctx.strokeStyle='#fff6d5';for(let x=0;x<=rc.w;x+=TILE){ctx.beginPath();ctx.moveTo(s.x+x,s.y);ctx.lineTo(s.x+x,s.y+rc.h);ctx.stroke()}for(let y=0;y<=rc.h;y+=TILE){ctx.beginPath();ctx.moveTo(s.x,s.y+y);ctx.lineTo(s.x+rc.w,s.y+y);ctx.stroke()}ctx.restore()}
function drawLighting(){const L=lighting();if(L.tint){ctx.fillStyle=L.tint;ctx.fillRect(0,0,VIEW.w,VIEW.h)}if(L.dark>0){ctx.fillStyle=`rgba(13,24,43,${L.dark})`;ctx.fillRect(0,0,VIEW.w,VIEW.h)}if(state.scene==='village'&&isNight()){for(const b of buildings){const r=tileRect(b),s=w2s(r.x+r.w*.5,r.y+r.h*.6);if(s.x<-120||s.x>VIEW.w+120)continue;const g=ctx.createRadialGradient(s.x,s.y,2,s.x,s.y,85);g.addColorStop(0,'rgba(255,215,120,.21)');g.addColorStop(1,'rgba(255,215,120,0)');ctx.fillStyle=g;ctx.fillRect(s.x-90,s.y-90,180,180)}}}
const rain=Array.from({length:130},()=>({x:Math.random()*1600,y:Math.random()*900,s:300+Math.random()*260,l:6+Math.random()*9}));
function updateRain(dt){if(state.weather!=='rain')return;for(const r of rain){r.y+=r.s*dt;r.x-=r.s*.22*dt;if(r.y>VIEW.h+20||r.x<-20){r.y=-20;r.x=Math.random()*VIEW.w+80}}}
function drawWeather(){if(state.weather==='rain'){ctx.save();ctx.strokeStyle='rgba(202,225,232,.55)';for(const r of rain){ctx.beginPath();ctx.moveTo(r.x,r.y);ctx.lineTo(r.x-r.l*.3,r.y+r.l);ctx.stroke()}ctx.restore()}else if(state.weather==='cloudy'){ctx.fillStyle='rgba(210,218,206,.05)';ctx.fillRect(0,0,VIEW.w,VIEW.h)}}
function drawFishingOverlay(){if(!fishing)return;const W=fishingCanvas.width,H=fishingCanvas.height,cx=W/2,cy=H/2,R=174;fctx.clearRect(0,0,W,H);fctx.fillStyle='#10282ee8';fctx.beginPath();fctx.arc(cx,cy,R+62,0,Math.PI*2);fctx.fill();fctx.strokeStyle='#d9bd73';fctx.lineWidth=5;fctx.stroke();fctx.strokeStyle='#365960';fctx.lineWidth=27;fctx.beginPath();fctx.arc(cx,cy,R,0,Math.PI*2);fctx.stroke();fctx.strokeStyle='#f0cf70';fctx.lineWidth=31;fctx.lineCap='round';fctx.beginPath();fctx.arc(cx,cy,R,fishing.barAngle-fishing.zone/2,fishing.barAngle+fishing.zone/2);fctx.stroke();const x=cx+Math.cos(fishing.fishAngle)*R,y=cy+Math.sin(fishing.fishAngle)*R;fctx.save();fctx.translate(x,y);fctx.rotate(fishing.fishAngle+Math.PI/2);fctx.fillStyle=fishing.species.rarity==='고급'?'#76ccd7':'#e5b75f';fctx.fillRect(-13,-7,26,14);fctx.beginPath();fctx.moveTo(-13,0);fctx.lineTo(-23,-11);fctx.lineTo(-23,11);fctx.closePath();fctx.fill();fctx.restore();fctx.fillStyle='#fff0c7';fctx.font='18px monospace';fctx.textAlign='center';fctx.fillText(fishing.result?(fishing.result.ok?'CATCH!':'MISS!'):'360° CATCH',cx,cy+6);fctx.font='11px monospace';fctx.fillStyle='#c3ddd7';fctx.fillText(`${DEPTH[cast.depth]?.label||''} · ${currentWeather().name} · ${isNight()?'밤':'낮'}`,cx,cy+31)}
function render(){ctx.clearRect(0,0,VIEW.w,VIEW.h);if(state.scene==='village')drawVillage();else if(state.scene==='shop')drawShop();else drawAquarium();drawBuildGhost();drawLighting();drawWeather();drawFishingOverlay()}

function resizeViewport(){const r=shell.getBoundingClientRect(),aspect=clamp(r.width/Math.max(1,r.height),CONFIG.minAspect,CONFIG.maxAspect);VIEW.h=CONFIG.baseH;VIEW.w=Math.round(VIEW.h*aspect);canvas.width=VIEW.w;canvas.height=VIEW.h;ctx.imageSmoothingEnabled=false}
function updateCamera(dt){const s=sceneSize(),tx=clamp(state.player.x-VIEW.w/2,0,Math.max(0,s.w-VIEW.w)),ty=clamp(state.player.y-VIEW.h/2,0,Math.max(0,s.h-VIEW.h)),t=1-Math.exp(-7*dt);camera.x=lerp(camera.x,tx,t);camera.y=lerp(camera.y,ty,t)}
function fixedUpdate(dt){if(!fishing&&!ui.modal){advanceTime(dt);updatePlayer(dt);updateNpcs(dt);updateVisitors(dt);updateCast(dt)}updateFishing(dt);updateCamera(dt);updateRain(dt);updateToast(dt);saveClock+=dt;if(saveClock>=6){saveClock=0;saveGame()}refreshHud()}

function keyCode(e){if(['KeyW','KeyA','KeyS','KeyD'].includes(e.code))return e.code;const k=String(e.key||'').toLowerCase();return k==='w'?'KeyW':k==='a'?'KeyA':k==='s'?'KeyS':k==='d'?'KeyD':null}
document.addEventListener('keydown',e=>{const c=keyCode(e);if(c){input.keys.add(c);e.preventDefault();return}if(e.code==='Escape'){if(buildState.active)cancelPlacement();else if(ui.modal)closeModal();else if(cast.mode==='charging')resetCast();e.preventDefault()}},true);
document.addEventListener('keyup',e=>{const c=keyCode(e);if(c){input.keys.delete(c);e.preventDefault()}},true);
window.addEventListener('blur',()=>{input.keys.clear();input.pointerDown=false});
canvas.tabIndex=0;
canvas.addEventListener('pointermove',pointerWorld);
canvas.addEventListener('pointerdown',e=>{if(e.button!==0)return;pointerWorld(e);input.pointerDown=true;canvas.focus({preventScroll:true});if(buildState.active){confirmPlacement();e.preventDefault();return}if(fishing||ui.modal)return;if(targetAtPointer())return;if(cast.mode==='idle'&&startCast())e.preventDefault()});
canvas.addEventListener('pointerup',e=>{if(e.button!==0)return;pointerWorld(e);input.pointerDown=false;if(fishing)return;if(buildState.active)return;if(cast.mode==='charging'){releaseCast();return}if(cast.mode==='waiting')return;handleWorldClick()});
canvas.addEventListener('contextmenu',e=>e.preventDefault());
dom.fishingOverlay.addEventListener('pointerdown',e=>{if(e.button===0)input.pointerDown=true});dom.fishingOverlay.addEventListener('pointerup',e=>{if(e.button===0)input.pointerDown=false});dom.iceBombButton.addEventListener('pointerdown',e=>{e.stopPropagation();input.pointerDown=false;useIceBomb()});dom.modalClose.addEventListener('click',()=>closeModal());dom.modalBackdrop.addEventListener('click',()=>closeModal());
document.querySelectorAll('[data-panel]').forEach(btn=>btn.addEventListener('click',()=>{const p=btn.dataset.panel;if(p==='inventory')openInventory();else if(p==='codex')openCodex();else if(p==='equipment')openEquipment();else if(p==='aquarium')openAquariumStatus()}));
dom.buildButton.addEventListener('click',openBuildMenu);dom.rotateBuild.addEventListener('click',rotatePlacement);dom.cancelBuild.addEventListener('click',cancelPlacement);
dom.resetButton.addEventListener('click',()=>{if(!confirm('낚시 아쿠아리움 v3.1 저장 데이터를 초기화할까요?'))return;localStorage.removeItem(CONFIG.saveKey);normalizeState(defaultState());npcs.splice(0,npcs.length,...npcDefs.map(d=>({...d,path:[],pathIndex:0,repath:Math.random()*2,dir:'down',moving:false})));camera.x=camera.y=0;saveGame();showToast('저장 데이터를 초기화했습니다.');refreshHud(true)});
window.addEventListener('beforeunload',saveGame);window.addEventListener('resize',()=>{resizeViewport();updateCamera(1)});

loadGame();resizeViewport();refreshHud(true);showToast('연못 마을 v3.1 · WASD 이동 / 좌클릭 상호작용');canvas.focus({preventScroll:true});
let last=performance.now(),acc=0;function frame(now){const delta=Math.min(CONFIG.maxFrameDelta,Math.max(0,(now-last)/1000));last=now;acc+=delta;let steps=0;while(acc>=CONFIG.fixedStep&&steps<10){fixedUpdate(CONFIG.fixedStep);acc-=CONFIG.fixedStep;steps++}if(steps>=10)acc=0;render();requestAnimationFrame(frame)}requestAnimationFrame(frame);
})();
